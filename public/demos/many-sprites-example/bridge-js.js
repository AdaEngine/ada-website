// NOTICE: This is auto-generated code by BridgeJS from JavaScriptKit,
// DO NOT EDIT.
//
// To update this file, just rebuild your project or run
// `swift package bridge-js`.

export const GPUCanvasAlphaModeValues = {
    Opaque: "opaque",
    Premultiplied: "premultiplied",
};

export const GPUPrimitiveTopologyValues = {
    PointList: "point-list",
    LineList: "line-list",
    LineStrip: "line-strip",
    TriangleList: "triangle-list",
    TriangleStrip: "triangle-strip",
};

export const GPUVertexFormatValues = {
    Uint8x2: "uint8x2",
    Uint8x4: "uint8x4",
    Sint8x2: "sint8x2",
    Sint8x4: "sint8x4",
    Unorm8x2: "unorm8x2",
    Unorm8x4: "unorm8x4",
    Snorm8x2: "snorm8x2",
    Snorm8x4: "snorm8x4",
    Float16x2: "float16x2",
    Float16x4: "float16x4",
    Float32: "float32",
    Float32x2: "float32x2",
    Float32x3: "float32x3",
    Float32x4: "float32x4",
    Uint32: "uint32",
    Uint32x2: "uint32x2",
    Uint32x3: "uint32x3",
    Uint32x4: "uint32x4",
    Sint32: "sint32",
    Sint32x2: "sint32x2",
    Sint32x3: "sint32x3",
    Sint32x4: "sint32x4",
};

export const GPUVertexStepModeValues = {
    Vertex: "vertex",
    Instance: "instance",
};

export const GPUTextureFormatValues = {
    Undefined: "",
    R8Unorm: "r8unorm",
    R8Snorm: "r8snorm",
    R8Uint: "r8uint",
    R8Sint: "r8sint",
    R16Unorm: "r16unorm",
    R16Snorm: "r16snorm",
    R16Uint: "r16uint",
    R16Sint: "r16sint",
    R16Float: "r16float",
    RG8Unorm: "rg8unorm",
    RG8Snorm: "rg8snorm",
    RG8Uint: "rg8uint",
    RG8Sint: "rg8sint",
    R32Float: "r32float",
    R32Uint: "r32uint",
    R32Sint: "r32sint",
    RG16Unorm: "rg16unorm",
    RG16Snorm: "rg16snorm",
    RG16Uint: "rg16uint",
    RG16Sint: "rg16sint",
    RG16Float: "rg16float",
    RGBA8Unorm: "rgba8unorm",
    RGBA8UnormSrgb: "rgba8unorm-srgb",
    RGBA8Snorm: "rgba8snorm",
    RGBA8Uint: "rgba8uint",
    RGBA8Sint: "rgba8sint",
    BGRA8Unorm: "bgra8unorm",
    BGRA8UnormSrgb: "bgra8unorm-srgb",
    RGB10A2Uint: "rgb10a2uint",
    RGB10A2Unorm: "rgb10a2unorm",
    RG11B10Ufloat: "rg11b10ufloat",
    RGB9E5Ufloat: "rgb9e5ufloat",
    RG32Float: "rg32float",
    RG32Uint: "rg32uint",
    RG32Sint: "rg32sint",
    RGBA16Unorm: "rgba16unorm",
    RGBA16Snorm: "rgba16snorm",
    RGBA16Uint: "rgba16uint",
    RGBA16Sint: "rgba16sint",
    RGBA16Float: "rgba16float",
    RGBA32Float: "rgba32float",
    RGBA32Uint: "rgba32uint",
    RGBA32Sint: "rgba32sint",
    Stencil8: "stencil8",
    Depth16Unorm: "depth16unorm",
    Depth24Plus: "depth24plus",
    Depth24PlusStencil8: "depth24plus-stencil8",
    Depth32Float: "depth32float",
    Depth32FloatStencil8: "depth32float-stencil8",
    BC1RGBAUnorm: "bc1-rgba-unorm",
    BC1RGBAUnormSrgb: "bc1-rgba-unorm-srgb",
    BC2RGBAUnorm: "bc2-rgba-unorm",
    BC2RGBAUnormSrgb: "bc2-rgba-unorm-srgb",
    BC3RGBAUnorm: "bc3-rgba-unorm",
    BC3RGBAUnormSrgb: "bc3-rgba-unorm-srgb",
    BC4RUnorm: "bc4-r-unorm",
    BC4RSnorm: "bc4-r-snorm",
    BC5RGUnorm: "bc5-rg-unorm",
    BC5RGSnorm: "bc5-rg-snorm",
    BC6HRGBUfloat: "bc6h-rgb-ufloat",
    BC6HRGBFloat: "bc6h-rgb-float",
    BC7RGBAUnorm: "bc7-rgba-unorm",
    BC7RGBAUnormSrgb: "bc7-rgba-unorm-srgb",
    ETC2RGB8Unorm: "etc2-rgb8unorm",
    ETC2RGB8UnormSrgb: "etc2-rgb8unorm-srgb",
    ETC2RGB8A1Unorm: "etc2-rgb8a1unorm",
    ETC2RGB8A1UnormSrgb: "etc2-rgb8a1unorm-srgb",
    ETC2RGBA8Unorm: "etc2-rgba8unorm",
    ETC2RGBA8UnormSrgb: "etc2-rgba8unorm-srgb",
    EACR11Unorm: "eac-r11unorm",
    EACR11Snorm: "eac-r11snorm",
    EACRG11Unorm: "eac-rg11unorm",
    EACRG11Snorm: "eac-rg11snorm",
    ASTC4x4Unorm: "astc-4x4-unorm",
    ASTC4x4UnormSrgb: "astc-4x4-unorm-srgb",
    ASTC5x4Unorm: "astc-5x4-unorm",
    ASTC5x4UnormSrgb: "astc-5x4-unorm-srgb",
    ASTC5x5Unorm: "astc-5x5-unorm",
    ASTC5x5UnormSrgb: "astc-5x5-unorm-srgb",
    ASTC6x5Unorm: "astc-6x5-unorm",
    ASTC6x5UnormSrgb: "astc-6x5-unorm-srgb",
    ASTC6x6Unorm: "astc-6x6-unorm",
    ASTC6x6UnormSrgb: "astc-6x6-unorm-srgb",
    ASTC8x5Unorm: "astc-8x5-unorm",
    ASTC8x5UnormSrgb: "astc-8x5-unorm-srgb",
    ASTC8x6Unorm: "astc-8x6-unorm",
    ASTC8x6UnormSrgb: "astc-8x6-unorm-srgb",
    ASTC8x8Unorm: "astc-8x8-unorm",
    ASTC8x8UnormSrgb: "astc-8x8-unorm-srgb",
    ASTC10x5Unorm: "astc-10x5-unorm",
    ASTC10x5UnormSrgb: "astc-10x5-unorm-srgb",
    ASTC10x6Unorm: "astc-10x6-unorm",
    ASTC10x6UnormSrgb: "astc-10x6-unorm-srgb",
    ASTC10x8Unorm: "astc-10x8-unorm",
    ASTC10x8UnormSrgb: "astc-10x8-unorm-srgb",
    ASTC10x10Unorm: "astc-10x10-unorm",
    ASTC10x10UnormSrgb: "astc-10x10-unorm-srgb",
    ASTC12x10Unorm: "astc-12x10-unorm",
    ASTC12x10UnormSrgb: "astc-12x10-unorm-srgb",
    ASTC12x12Unorm: "astc-12x12-unorm",
    ASTC12x12UnormSrgb: "astc-12x12-unorm-srgb",
    Force32: "force32",
};

export const GPULoadOpValues = {
    Load: "load",
    Clear: "clear",
};

export const GPUStoreOpValues = {
    Store: "store",
    Discard: "discard",
};

export const GPUFrontFaceValues = {
    CCW: "ccw",
    CW: "cw",
};

export const GPUCullModeValues = {
    None: "none",
    Front: "front",
    Back: "back",
};

export const GPUIndexFormatValues = {
    Undefined: "undefined",
    Uint16: "uint16",
    Uint32: "uint32",
    Force32: "force32",
};

export const GPUBufferBindingTypeValues = {
    Uniform: "uniform",
    Storage: "storage",
    ReadOnlyStorage: "read-only-storage",
};

export const GPUTextureViewDimensionValues = {
    _1D: "1d",
    _2D: "2d",
    _2DArray: "2d-array",
    Cube: "cube",
    CubeArray: "cube-array",
    _3D: "3d",
};

export const GPUTextureSampleTypeValues = {
    Float: "float",
    UnfilterableFloat: "unfilterable-float",
    Depth: "depth",
    Sint: "sint",
    Uint: "uint",
};

export const GPUStorageTextureAccessValues = {
    WriteOnly: "write-only",
    ReadOnly: "read-only",
    ReadWrite: "read-write",
};

export const GPUSamplerBindingTypeValues = {
    Filtering: "filtering",
    NonFiltering: "non-filtering",
    Comparison: "comparison",
};

export const GPUQueryTypeValues = {
    Occlusion: "occlusion",
    Timestamp: "timestamp",
};

export const GPUAddressModeValues = {
    ClampToEdge: "clamp-to-edge",
    RepeatMode: "repeat",
    MirrorRepeat: "mirror-repeat",
};

export const GPUFilterModeValues = {
    Nearest: "nearest",
    Linear: "linear",
};

export const GPUMipmapFilterModeValues = {
    Nearest: "nearest",
    Linear: "linear",
};

export const GPUCompareFunctionValues = {
    Never: "never",
    Less: "less",
    Equal: "equal",
    LessEqual: "less-equal",
    Greater: "greater",
    NotEqual: "not-equal",
    GreaterEqual: "greater-equal",
    Always: "always",
};

export const GPUBlendFactorValues = {
    Zero: "zero",
    One: "one",
    Src: "src",
    OneMinusSrc: "one-minus-src",
    SrcAlpha: "src-alpha",
    OneMinusSrcAlpha: "one-minus-src-alpha",
    Dst: "dst",
    OneMinusDst: "one-minus-dst",
    DstAlpha: "dst-alpha",
    OneMinusDstAlpha: "one-minus-dst-alpha",
    SrcAlphaSaturated: "src-alpha-saturated",
    Constant: "constant",
    OneMinusConstant: "one-minus-constant",
    Src1: "src1",
    OneMinusSrc1: "one-minus-src1",
    Src1Alpha: "src1-alpha",
    OneMinusSrc1Alpha: "one-minus-src1-alpha",
};

export const GPUBlendOperationValues = {
    Add: "add",
    Subtract: "subtract",
    ReverseSubtract: "reverse-subtract",
    Min: "min",
    Max: "max",
};

export const GPUTextureAspectValues = {
    All: "all",
    StencilOnly: "stencil-only",
    DepthOnly: "depth-only",
};

export const GPUTextureDimensionValues = {
    _1D: "1d",
    _2D: "2d",
    _3D: "3d",
};

export const GPUStencilOperationValues = {
    Keep: "keep",
    Zero: "zero",
    Replace: "replace",
    Invert: "invert",
    IncrementClamp: "increment-clamp",
    DecrementClamp: "decrement-clamp",
    IncrementWrap: "increment-wrap",
    DecrementWrap: "decrement-wrap",
};

export async function createInstantiator(options, swift) {
    let instance;
    let memory;
    let setException;
    let decodeString;
    const textDecoder = new TextDecoder("utf-8");
    const textEncoder = new TextEncoder("utf-8");
    let tmpRetString;
    let tmpRetBytes;
    let tmpRetException;
    let tmpRetOptionalBool;
    let tmpRetOptionalInt;
    let tmpRetOptionalFloat;
    let tmpRetOptionalDouble;
    let tmpRetOptionalHeapObject;
    let strStack = [];
    let i32Stack = [];
    let f32Stack = [];
    let f64Stack = [];
    let ptrStack = [];
    const enumHelpers = {};
    const structHelpers = {};

    let _exports = null;
    let bjs = null;
    const __bjs_createGPUBufferDescriptorHelpers = () => ({
        lower: (value) => {
            const isSome = value.label != null ? 1 : 0;
            if (isSome) {
                const bytes = textEncoder.encode(value.label);
                const id = swift.memory.retain(bytes);
                i32Stack.push(bytes.length);
                i32Stack.push(id);
            }
            i32Stack.push(isSome);
            i32Stack.push((value.usage | 0));
            i32Stack.push((value.size | 0));
            i32Stack.push(value.mappedAtCreation ? 1 : 0);
        },
        lift: () => {
            const bool = i32Stack.pop() !== 0;
            const int = i32Stack.pop();
            const int1 = i32Stack.pop();
            const isSome = i32Stack.pop();
            let optValue;
            if (isSome === 0) {
                optValue = null;
            } else {
                const string = strStack.pop();
                optValue = string;
            }
            return { label: optValue, usage: int1, size: int, mappedAtCreation: bool };
        }
    });
    const __bjs_createGPUShaderModuleDescriptorHelpers = () => ({
        lower: (value) => {
            const isSome = value.label != null ? 1 : 0;
            if (isSome) {
                const bytes = textEncoder.encode(value.label);
                const id = swift.memory.retain(bytes);
                i32Stack.push(bytes.length);
                i32Stack.push(id);
            }
            i32Stack.push(isSome);
            const bytes1 = textEncoder.encode(value.code);
            const id1 = swift.memory.retain(bytes1);
            i32Stack.push(bytes1.length);
            i32Stack.push(id1);
        },
        lift: () => {
            const string = strStack.pop();
            const isSome = i32Stack.pop();
            let optValue;
            if (isSome === 0) {
                optValue = null;
            } else {
                const string1 = strStack.pop();
                optValue = string1;
            }
            return { label: optValue, code: string };
        }
    });
    const __bjs_createGPUVertexAttributeHelpers = () => ({
        lower: (value) => {
            const bytes = textEncoder.encode(value.format);
            const id = swift.memory.retain(bytes);
            i32Stack.push(bytes.length);
            i32Stack.push(id);
            i32Stack.push((value.offset | 0));
            i32Stack.push((value.shaderLocation | 0));
        },
        lift: () => {
            const int = i32Stack.pop();
            const int1 = i32Stack.pop();
            const rawValue = strStack.pop();
            return { format: rawValue, offset: int1, shaderLocation: int };
        }
    });
    const __bjs_createGPUVertexBufferLayoutHelpers = () => ({
        lower: (value) => {
            i32Stack.push((value.arrayStride | 0));
            const bytes = textEncoder.encode(value.stepMode);
            const id = swift.memory.retain(bytes);
            i32Stack.push(bytes.length);
            i32Stack.push(id);
            for (const elem of value.attributes) {
                structHelpers.GPUVertexAttribute.lower(elem);
            }
            i32Stack.push(value.attributes.length);
        },
        lift: () => {
            const arrayLen = i32Stack.pop();
            const arrayResult = [];
            for (let i = 0; i < arrayLen; i++) {
                const struct = structHelpers.GPUVertexAttribute.lift();
                arrayResult.push(struct);
            }
            arrayResult.reverse();
            const rawValue = strStack.pop();
            const int = i32Stack.pop();
            return { arrayStride: int, stepMode: rawValue, attributes: arrayResult };
        }
    });
    const __bjs_createGPUPrimitiveStateHelpers = () => ({
        lower: (value) => {
            const bytes = textEncoder.encode(value.topology);
            const id = swift.memory.retain(bytes);
            i32Stack.push(bytes.length);
            i32Stack.push(id);
            const bytes1 = textEncoder.encode(value.stripIndexFormat);
            const id1 = swift.memory.retain(bytes1);
            i32Stack.push(bytes1.length);
            i32Stack.push(id1);
            const bytes2 = textEncoder.encode(value.frontFace);
            const id2 = swift.memory.retain(bytes2);
            i32Stack.push(bytes2.length);
            i32Stack.push(id2);
            const bytes3 = textEncoder.encode(value.cullMode);
            const id3 = swift.memory.retain(bytes3);
            i32Stack.push(bytes3.length);
            i32Stack.push(id3);
        },
        lift: () => {
            const rawValue = strStack.pop();
            const rawValue1 = strStack.pop();
            const rawValue2 = strStack.pop();
            const rawValue3 = strStack.pop();
            return { topology: rawValue3, stripIndexFormat: rawValue2, frontFace: rawValue1, cullMode: rawValue };
        }
    });
    const __bjs_createGPUBlendComponentHelpers = () => ({
        lower: (value) => {
            const bytes = textEncoder.encode(value.operation);
            const id = swift.memory.retain(bytes);
            i32Stack.push(bytes.length);
            i32Stack.push(id);
            const bytes1 = textEncoder.encode(value.srcFactor);
            const id1 = swift.memory.retain(bytes1);
            i32Stack.push(bytes1.length);
            i32Stack.push(id1);
            const bytes2 = textEncoder.encode(value.dstFactor);
            const id2 = swift.memory.retain(bytes2);
            i32Stack.push(bytes2.length);
            i32Stack.push(id2);
        },
        lift: () => {
            const rawValue = strStack.pop();
            const rawValue1 = strStack.pop();
            const rawValue2 = strStack.pop();
            return { operation: rawValue2, srcFactor: rawValue1, dstFactor: rawValue };
        }
    });
    const __bjs_createGPUBlendStateHelpers = () => ({
        lower: (value) => {
            structHelpers.GPUBlendComponent.lower(value.color);
            structHelpers.GPUBlendComponent.lower(value.alpha);
        },
        lift: () => {
            const struct = structHelpers.GPUBlendComponent.lift();
            const struct1 = structHelpers.GPUBlendComponent.lift();
            return { color: struct1, alpha: struct };
        }
    });
    const __bjs_createGPUColorTargetStateHelpers = () => ({
        lower: (value) => {
            const bytes = textEncoder.encode(value.format);
            const id = swift.memory.retain(bytes);
            i32Stack.push(bytes.length);
            i32Stack.push(id);
            const isSome = value.blend != null ? 1 : 0;
            if (isSome) {
                structHelpers.GPUBlendState.lower(value.blend);
            }
            i32Stack.push(isSome);
            i32Stack.push((value.writeMask | 0));
        },
        lift: () => {
            const int = i32Stack.pop();
            const isSome = i32Stack.pop();
            let optValue;
            if (isSome === 0) {
                optValue = null;
            } else {
                const struct = structHelpers.GPUBlendState.lift();
                optValue = struct;
            }
            const rawValue = strStack.pop();
            return { format: rawValue, blend: optValue, writeMask: int };
        }
    });
    const __bjs_createGPUColorHelpers = () => ({
        lower: (value) => {
            f64Stack.push(value.r);
            f64Stack.push(value.g);
            f64Stack.push(value.b);
            f64Stack.push(value.a);
        },
        lift: () => {
            const f64 = f64Stack.pop();
            const f641 = f64Stack.pop();
            const f642 = f64Stack.pop();
            const f643 = f64Stack.pop();
            return { r: f643, g: f642, b: f641, a: f64 };
        }
    });
    const __bjs_createGPUCommandEncoderDescriptorHelpers = () => ({
        lower: (value) => {
            const isSome = value.label != null ? 1 : 0;
            if (isSome) {
                const bytes = textEncoder.encode(value.label);
                const id = swift.memory.retain(bytes);
                i32Stack.push(bytes.length);
                i32Stack.push(id);
            }
            i32Stack.push(isSome);
        },
        lift: () => {
            const isSome = i32Stack.pop();
            let optValue;
            if (isSome === 0) {
                optValue = null;
            } else {
                const string = strStack.pop();
                optValue = string;
            }
            return { label: optValue };
        }
    });
    const __bjs_createGPUCommandBufferDescriptorHelpers = () => ({
        lower: (value) => {
            const isSome = value.label != null ? 1 : 0;
            if (isSome) {
                const bytes = textEncoder.encode(value.label);
                const id = swift.memory.retain(bytes);
                i32Stack.push(bytes.length);
                i32Stack.push(id);
            }
            i32Stack.push(isSome);
        },
        lift: () => {
            const isSome = i32Stack.pop();
            let optValue;
            if (isSome === 0) {
                optValue = null;
            } else {
                const string = strStack.pop();
                optValue = string;
            }
            return { label: optValue };
        }
    });
    const __bjs_createGPURenderPassColorAttachmentHelpers = () => ({
        lower: (value) => {
            let id;
            if (value.view != null) {
                id = swift.memory.retain(value.view);
            } else {
                id = undefined;
            }
            i32Stack.push(id !== undefined ? id : 0);
            const bytes = textEncoder.encode(value.loadOp);
            const id1 = swift.memory.retain(bytes);
            i32Stack.push(bytes.length);
            i32Stack.push(id1);
            const bytes1 = textEncoder.encode(value.storeOp);
            const id2 = swift.memory.retain(bytes1);
            i32Stack.push(bytes1.length);
            i32Stack.push(id2);
            const isSome = value.clearValue != null ? 1 : 0;
            if (isSome) {
                structHelpers.GPUColor.lower(value.clearValue);
            }
            i32Stack.push(isSome);
        },
        lift: () => {
            const isSome = i32Stack.pop();
            let optValue;
            if (isSome === 0) {
                optValue = null;
            } else {
                const struct = structHelpers.GPUColor.lift();
                optValue = struct;
            }
            const rawValue = strStack.pop();
            const rawValue1 = strStack.pop();
            const objectId = i32Stack.pop();
            let value;
            if (objectId !== 0) {
                value = swift.memory.getObject(objectId);
                swift.memory.release(objectId);
            } else {
                value = null;
            }
            return { view: value, loadOp: rawValue1, storeOp: rawValue, clearValue: optValue };
        }
    });
    const __bjs_createGPURenderPassDepthStencilAttachmentHelpers = () => ({
        lower: (value) => {
            let id;
            if (value.view != null) {
                id = swift.memory.retain(value.view);
            } else {
                id = undefined;
            }
            i32Stack.push(id !== undefined ? id : 0);
            const isSome = value.depthLoadOp != null ? 1 : 0;
            if (isSome) {
                const bytes = textEncoder.encode(value.depthLoadOp);
                const id1 = swift.memory.retain(bytes);
                i32Stack.push(bytes.length);
                i32Stack.push(id1);
            }
            i32Stack.push(isSome);
            const isSome1 = value.depthStoreOp != null ? 1 : 0;
            if (isSome1) {
                const bytes1 = textEncoder.encode(value.depthStoreOp);
                const id2 = swift.memory.retain(bytes1);
                i32Stack.push(bytes1.length);
                i32Stack.push(id2);
            }
            i32Stack.push(isSome1);
            f64Stack.push(value.depthClearValue);
            i32Stack.push(value.depthReadOnly ? 1 : 0);
            const isSome2 = value.stencilLoadOp != null ? 1 : 0;
            if (isSome2) {
                const bytes2 = textEncoder.encode(value.stencilLoadOp);
                const id3 = swift.memory.retain(bytes2);
                i32Stack.push(bytes2.length);
                i32Stack.push(id3);
            }
            i32Stack.push(isSome2);
            const isSome3 = value.stencilStoreOp != null ? 1 : 0;
            if (isSome3) {
                const bytes3 = textEncoder.encode(value.stencilStoreOp);
                const id4 = swift.memory.retain(bytes3);
                i32Stack.push(bytes3.length);
                i32Stack.push(id4);
            }
            i32Stack.push(isSome3);
            i32Stack.push((value.stencilClearValue | 0));
            i32Stack.push(value.stencilReadOnly ? 1 : 0);
        },
        lift: () => {
            const bool = i32Stack.pop() !== 0;
            const int = i32Stack.pop();
            const isSome = i32Stack.pop();
            let optValue;
            if (isSome === 0) {
                optValue = null;
            } else {
                const rawValue = strStack.pop();
                optValue = rawValue;
            }
            const isSome1 = i32Stack.pop();
            let optValue1;
            if (isSome1 === 0) {
                optValue1 = null;
            } else {
                const rawValue1 = strStack.pop();
                optValue1 = rawValue1;
            }
            const bool1 = i32Stack.pop() !== 0;
            const f64 = f64Stack.pop();
            const isSome2 = i32Stack.pop();
            let optValue2;
            if (isSome2 === 0) {
                optValue2 = null;
            } else {
                const rawValue2 = strStack.pop();
                optValue2 = rawValue2;
            }
            const isSome3 = i32Stack.pop();
            let optValue3;
            if (isSome3 === 0) {
                optValue3 = null;
            } else {
                const rawValue3 = strStack.pop();
                optValue3 = rawValue3;
            }
            const objectId = i32Stack.pop();
            let value;
            if (objectId !== 0) {
                value = swift.memory.getObject(objectId);
                swift.memory.release(objectId);
            } else {
                value = null;
            }
            return { view: value, depthLoadOp: optValue3, depthStoreOp: optValue2, depthClearValue: f64, depthReadOnly: bool1, stencilLoadOp: optValue1, stencilStoreOp: optValue, stencilClearValue: int, stencilReadOnly: bool };
        }
    });
    const __bjs_createGPURenderPassDescriptorHelpers = () => ({
        lower: (value) => {
            const isSome = value.label != null ? 1 : 0;
            if (isSome) {
                const bytes = textEncoder.encode(value.label);
                const id = swift.memory.retain(bytes);
                i32Stack.push(bytes.length);
                i32Stack.push(id);
            }
            i32Stack.push(isSome);
            for (const elem of value.colorAttachments) {
                structHelpers.GPURenderPassColorAttachment.lower(elem);
            }
            i32Stack.push(value.colorAttachments.length);
            const isSome1 = value.depthStencilAttachment != null ? 1 : 0;
            if (isSome1) {
                structHelpers.GPURenderPassDepthStencilAttachment.lower(value.depthStencilAttachment);
            }
            i32Stack.push(isSome1);
            const isSome2 = value.timestampWrites != null ? 1 : 0;
            if (isSome2) {
                structHelpers.GPUPassTimestampWrites.lower(value.timestampWrites);
            }
            i32Stack.push(isSome2);
        },
        lift: () => {
            const isSome = i32Stack.pop();
            let optValue;
            if (isSome === 0) {
                optValue = null;
            } else {
                const struct = structHelpers.GPUPassTimestampWrites.lift();
                optValue = struct;
            }
            const isSome1 = i32Stack.pop();
            let optValue1;
            if (isSome1 === 0) {
                optValue1 = null;
            } else {
                const struct1 = structHelpers.GPURenderPassDepthStencilAttachment.lift();
                optValue1 = struct1;
            }
            const arrayLen = i32Stack.pop();
            const arrayResult = [];
            for (let i = 0; i < arrayLen; i++) {
                const struct2 = structHelpers.GPURenderPassColorAttachment.lift();
                arrayResult.push(struct2);
            }
            arrayResult.reverse();
            const isSome2 = i32Stack.pop();
            let optValue2;
            if (isSome2 === 0) {
                optValue2 = null;
            } else {
                const string = strStack.pop();
                optValue2 = string;
            }
            return { label: optValue2, colorAttachments: arrayResult, depthStencilAttachment: optValue1, timestampWrites: optValue };
        }
    });
    const __bjs_createGPUComputePassDescriptorHelpers = () => ({
        lower: (value) => {
            const isSome = value.label != null ? 1 : 0;
            if (isSome) {
                const bytes = textEncoder.encode(value.label);
                const id = swift.memory.retain(bytes);
                i32Stack.push(bytes.length);
                i32Stack.push(id);
            }
            i32Stack.push(isSome);
            const isSome1 = value.timestampWrites != null ? 1 : 0;
            if (isSome1) {
                structHelpers.GPUPassTimestampWrites.lower(value.timestampWrites);
            }
            i32Stack.push(isSome1);
        },
        lift: () => {
            const isSome = i32Stack.pop();
            let optValue;
            if (isSome === 0) {
                optValue = null;
            } else {
                const struct = structHelpers.GPUPassTimestampWrites.lift();
                optValue = struct;
            }
            const isSome1 = i32Stack.pop();
            let optValue1;
            if (isSome1 === 0) {
                optValue1 = null;
            } else {
                const string = strStack.pop();
                optValue1 = string;
            }
            return { label: optValue1, timestampWrites: optValue };
        }
    });
    const __bjs_createGPUStencilFaceStateHelpers = () => ({
        lower: (value) => {
            const bytes = textEncoder.encode(value.compare);
            const id = swift.memory.retain(bytes);
            i32Stack.push(bytes.length);
            i32Stack.push(id);
            const bytes1 = textEncoder.encode(value.failOp);
            const id1 = swift.memory.retain(bytes1);
            i32Stack.push(bytes1.length);
            i32Stack.push(id1);
            const bytes2 = textEncoder.encode(value.depthFailOp);
            const id2 = swift.memory.retain(bytes2);
            i32Stack.push(bytes2.length);
            i32Stack.push(id2);
            const bytes3 = textEncoder.encode(value.passOp);
            const id3 = swift.memory.retain(bytes3);
            i32Stack.push(bytes3.length);
            i32Stack.push(id3);
        },
        lift: () => {
            const rawValue = strStack.pop();
            const rawValue1 = strStack.pop();
            const rawValue2 = strStack.pop();
            const rawValue3 = strStack.pop();
            return { compare: rawValue3, failOp: rawValue2, depthFailOp: rawValue1, passOp: rawValue };
        }
    });
    const __bjs_createGPUDepthStencilStateHelpers = () => ({
        lower: (value) => {
            const bytes = textEncoder.encode(value.format);
            const id = swift.memory.retain(bytes);
            i32Stack.push(bytes.length);
            i32Stack.push(id);
            i32Stack.push(value.depthWriteEnabled ? 1 : 0);
            const bytes1 = textEncoder.encode(value.depthCompare);
            const id1 = swift.memory.retain(bytes1);
            i32Stack.push(bytes1.length);
            i32Stack.push(id1);
            structHelpers.GPUStencilFaceState.lower(value.stencilFront);
            structHelpers.GPUStencilFaceState.lower(value.stencilBack);
            i32Stack.push((value.stencilReadMask | 0));
            i32Stack.push((value.stencilWriteMask | 0));
            i32Stack.push((value.depthBias | 0));
            f64Stack.push(value.depthBiasSlopeScale);
            f64Stack.push(value.depthBiasClamp);
        },
        lift: () => {
            const f64 = f64Stack.pop();
            const f641 = f64Stack.pop();
            const int = i32Stack.pop();
            const int1 = i32Stack.pop();
            const int2 = i32Stack.pop();
            const struct = structHelpers.GPUStencilFaceState.lift();
            const struct1 = structHelpers.GPUStencilFaceState.lift();
            const rawValue = strStack.pop();
            const bool = i32Stack.pop() !== 0;
            const rawValue1 = strStack.pop();
            return { format: rawValue1, depthWriteEnabled: bool, depthCompare: rawValue, stencilFront: struct1, stencilBack: struct, stencilReadMask: int2, stencilWriteMask: int1, depthBias: int, depthBiasSlopeScale: f641, depthBiasClamp: f64 };
        }
    });
    const __bjs_createGPUExtent3DHelpers = () => ({
        lower: (value) => {
            i32Stack.push((value.width | 0));
            i32Stack.push((value.height | 0));
            i32Stack.push((value.depthOrArrayLayers | 0));
        },
        lift: () => {
            const int = i32Stack.pop();
            const int1 = i32Stack.pop();
            const int2 = i32Stack.pop();
            return { width: int2, height: int1, depthOrArrayLayers: int };
        }
    });
    const __bjs_createGPUOrigin3DHelpers = () => ({
        lower: (value) => {
            i32Stack.push((value.x | 0));
            i32Stack.push((value.y | 0));
            i32Stack.push((value.z | 0));
        },
        lift: () => {
            const int = i32Stack.pop();
            const int1 = i32Stack.pop();
            const int2 = i32Stack.pop();
            return { x: int2, y: int1, z: int };
        }
    });
    const __bjs_createGPUTextureDescriptorHelpers = () => ({
        lower: (value) => {
            const isSome = value.label != null ? 1 : 0;
            if (isSome) {
                const bytes = textEncoder.encode(value.label);
                const id = swift.memory.retain(bytes);
                i32Stack.push(bytes.length);
                i32Stack.push(id);
            }
            i32Stack.push(isSome);
            structHelpers.GPUExtent3D.lower(value.size);
            i32Stack.push((value.mipLevelCount | 0));
            i32Stack.push((value.sampleCount | 0));
            const bytes1 = textEncoder.encode(value.dimension);
            const id1 = swift.memory.retain(bytes1);
            i32Stack.push(bytes1.length);
            i32Stack.push(id1);
            const bytes2 = textEncoder.encode(value.format);
            const id2 = swift.memory.retain(bytes2);
            i32Stack.push(bytes2.length);
            i32Stack.push(id2);
            i32Stack.push((value.usage | 0));
        },
        lift: () => {
            const int = i32Stack.pop();
            const rawValue = strStack.pop();
            const rawValue1 = strStack.pop();
            const int1 = i32Stack.pop();
            const int2 = i32Stack.pop();
            const struct = structHelpers.GPUExtent3D.lift();
            const isSome = i32Stack.pop();
            let optValue;
            if (isSome === 0) {
                optValue = null;
            } else {
                const string = strStack.pop();
                optValue = string;
            }
            return { label: optValue, size: struct, mipLevelCount: int2, sampleCount: int1, dimension: rawValue1, format: rawValue, usage: int };
        }
    });
    const __bjs_createGPUTextureViewDescriptorHelpers = () => ({
        lower: (value) => {
            const isSome = value.label != null ? 1 : 0;
            if (isSome) {
                const bytes = textEncoder.encode(value.label);
                const id = swift.memory.retain(bytes);
                i32Stack.push(bytes.length);
                i32Stack.push(id);
            }
            i32Stack.push(isSome);
            const isSome1 = value.format != null ? 1 : 0;
            if (isSome1) {
                const bytes1 = textEncoder.encode(value.format);
                const id1 = swift.memory.retain(bytes1);
                i32Stack.push(bytes1.length);
                i32Stack.push(id1);
            }
            i32Stack.push(isSome1);
            const isSome2 = value.dimension != null ? 1 : 0;
            if (isSome2) {
                const bytes2 = textEncoder.encode(value.dimension);
                const id2 = swift.memory.retain(bytes2);
                i32Stack.push(bytes2.length);
                i32Stack.push(id2);
            }
            i32Stack.push(isSome2);
            const bytes3 = textEncoder.encode(value.aspect);
            const id3 = swift.memory.retain(bytes3);
            i32Stack.push(bytes3.length);
            i32Stack.push(id3);
            i32Stack.push((value.baseMipLevel | 0));
            const isSome3 = value.mipLevelCount != null ? 1 : 0;
            if (isSome3) {
                i32Stack.push((value.mipLevelCount | 0));
            }
            i32Stack.push(isSome3);
            i32Stack.push((value.baseArrayLayer | 0));
            const isSome4 = value.arrayLayerCount != null ? 1 : 0;
            if (isSome4) {
                i32Stack.push((value.arrayLayerCount | 0));
            }
            i32Stack.push(isSome4);
        },
        lift: () => {
            const isSome = i32Stack.pop();
            let optValue;
            if (isSome === 0) {
                optValue = null;
            } else {
                const int = i32Stack.pop();
                optValue = int;
            }
            const int1 = i32Stack.pop();
            const isSome1 = i32Stack.pop();
            let optValue1;
            if (isSome1 === 0) {
                optValue1 = null;
            } else {
                const int2 = i32Stack.pop();
                optValue1 = int2;
            }
            const int3 = i32Stack.pop();
            const rawValue = strStack.pop();
            const isSome2 = i32Stack.pop();
            let optValue2;
            if (isSome2 === 0) {
                optValue2 = null;
            } else {
                const rawValue1 = strStack.pop();
                optValue2 = rawValue1;
            }
            const isSome3 = i32Stack.pop();
            let optValue3;
            if (isSome3 === 0) {
                optValue3 = null;
            } else {
                const rawValue2 = strStack.pop();
                optValue3 = rawValue2;
            }
            const isSome4 = i32Stack.pop();
            let optValue4;
            if (isSome4 === 0) {
                optValue4 = null;
            } else {
                const string = strStack.pop();
                optValue4 = string;
            }
            return { label: optValue4, format: optValue3, dimension: optValue2, aspect: rawValue, baseMipLevel: int3, mipLevelCount: optValue1, baseArrayLayer: int1, arrayLayerCount: optValue };
        }
    });
    const __bjs_createGPUSamplerDescriptorHelpers = () => ({
        lower: (value) => {
            const isSome = value.label != null ? 1 : 0;
            if (isSome) {
                const bytes = textEncoder.encode(value.label);
                const id = swift.memory.retain(bytes);
                i32Stack.push(bytes.length);
                i32Stack.push(id);
            }
            i32Stack.push(isSome);
            const bytes1 = textEncoder.encode(value.addressModeU);
            const id1 = swift.memory.retain(bytes1);
            i32Stack.push(bytes1.length);
            i32Stack.push(id1);
            const bytes2 = textEncoder.encode(value.addressModeV);
            const id2 = swift.memory.retain(bytes2);
            i32Stack.push(bytes2.length);
            i32Stack.push(id2);
            const bytes3 = textEncoder.encode(value.addressModeW);
            const id3 = swift.memory.retain(bytes3);
            i32Stack.push(bytes3.length);
            i32Stack.push(id3);
            const bytes4 = textEncoder.encode(value.magFilter);
            const id4 = swift.memory.retain(bytes4);
            i32Stack.push(bytes4.length);
            i32Stack.push(id4);
            const bytes5 = textEncoder.encode(value.minFilter);
            const id5 = swift.memory.retain(bytes5);
            i32Stack.push(bytes5.length);
            i32Stack.push(id5);
            const bytes6 = textEncoder.encode(value.mipmapFilter);
            const id6 = swift.memory.retain(bytes6);
            i32Stack.push(bytes6.length);
            i32Stack.push(id6);
            f64Stack.push(value.lodMinClamp);
            f64Stack.push(value.lodMaxClamp);
            const isSome1 = value.compare != null ? 1 : 0;
            if (isSome1) {
                const bytes7 = textEncoder.encode(value.compare);
                const id7 = swift.memory.retain(bytes7);
                i32Stack.push(bytes7.length);
                i32Stack.push(id7);
            }
            i32Stack.push(isSome1);
            i32Stack.push((value.maxAnisotropy | 0));
        },
        lift: () => {
            const int = i32Stack.pop();
            const isSome = i32Stack.pop();
            let optValue;
            if (isSome === 0) {
                optValue = null;
            } else {
                const rawValue = strStack.pop();
                optValue = rawValue;
            }
            const f64 = f64Stack.pop();
            const f641 = f64Stack.pop();
            const rawValue1 = strStack.pop();
            const rawValue2 = strStack.pop();
            const rawValue3 = strStack.pop();
            const rawValue4 = strStack.pop();
            const rawValue5 = strStack.pop();
            const rawValue6 = strStack.pop();
            const isSome1 = i32Stack.pop();
            let optValue1;
            if (isSome1 === 0) {
                optValue1 = null;
            } else {
                const string = strStack.pop();
                optValue1 = string;
            }
            return { label: optValue1, addressModeU: rawValue6, addressModeV: rawValue5, addressModeW: rawValue4, magFilter: rawValue3, minFilter: rawValue2, mipmapFilter: rawValue1, lodMinClamp: f641, lodMaxClamp: f64, compare: optValue, maxAnisotropy: int };
        }
    });
    const __bjs_createGPUQuerySetDescriptorHelpers = () => ({
        lower: (value) => {
            const isSome = value.label != null ? 1 : 0;
            if (isSome) {
                const bytes = textEncoder.encode(value.label);
                const id = swift.memory.retain(bytes);
                i32Stack.push(bytes.length);
                i32Stack.push(id);
            }
            i32Stack.push(isSome);
            const bytes1 = textEncoder.encode(value.type);
            const id1 = swift.memory.retain(bytes1);
            i32Stack.push(bytes1.length);
            i32Stack.push(id1);
            i32Stack.push((value.count | 0));
        },
        lift: () => {
            const int = i32Stack.pop();
            const rawValue = strStack.pop();
            const isSome = i32Stack.pop();
            let optValue;
            if (isSome === 0) {
                optValue = null;
            } else {
                const string = strStack.pop();
                optValue = string;
            }
            return { label: optValue, type: rawValue, count: int };
        }
    });
    const __bjs_createGPUPassTimestampWritesHelpers = () => ({
        lower: (value) => {
            let id;
            if (value.querySet != null) {
                id = swift.memory.retain(value.querySet);
            } else {
                id = undefined;
            }
            i32Stack.push(id !== undefined ? id : 0);
            i32Stack.push((value.beginningOfPassWriteIndex | 0));
            i32Stack.push((value.endOfPassWriteIndex | 0));
        },
        lift: () => {
            const int = i32Stack.pop();
            const int1 = i32Stack.pop();
            const objectId = i32Stack.pop();
            let value;
            if (objectId !== 0) {
                value = swift.memory.getObject(objectId);
                swift.memory.release(objectId);
            } else {
                value = null;
            }
            return { querySet: value, beginningOfPassWriteIndex: int1, endOfPassWriteIndex: int };
        }
    });
    const __bjs_createGPUBufferBindingLayoutHelpers = () => ({
        lower: (value) => {
            const bytes = textEncoder.encode(value.type);
            const id = swift.memory.retain(bytes);
            i32Stack.push(bytes.length);
            i32Stack.push(id);
            i32Stack.push(value.hasDynamicOffset ? 1 : 0);
            i32Stack.push((value.minBindingSize | 0));
        },
        lift: () => {
            const int = i32Stack.pop();
            const bool = i32Stack.pop() !== 0;
            const rawValue = strStack.pop();
            return { type: rawValue, hasDynamicOffset: bool, minBindingSize: int };
        }
    });
    const __bjs_createGPUSamplerBindingLayoutHelpers = () => ({
        lower: (value) => {
            const bytes = textEncoder.encode(value.type);
            const id = swift.memory.retain(bytes);
            i32Stack.push(bytes.length);
            i32Stack.push(id);
        },
        lift: () => {
            const rawValue = strStack.pop();
            return { type: rawValue };
        }
    });
    const __bjs_createGPUTextureBindingLayoutHelpers = () => ({
        lower: (value) => {
            const bytes = textEncoder.encode(value.sampleType);
            const id = swift.memory.retain(bytes);
            i32Stack.push(bytes.length);
            i32Stack.push(id);
            const bytes1 = textEncoder.encode(value.viewDimension);
            const id1 = swift.memory.retain(bytes1);
            i32Stack.push(bytes1.length);
            i32Stack.push(id1);
            i32Stack.push(value.multisampled ? 1 : 0);
        },
        lift: () => {
            const bool = i32Stack.pop() !== 0;
            const rawValue = strStack.pop();
            const rawValue1 = strStack.pop();
            return { sampleType: rawValue1, viewDimension: rawValue, multisampled: bool };
        }
    });
    const __bjs_createGPUStorageTextureBindingLayoutHelpers = () => ({
        lower: (value) => {
            const bytes = textEncoder.encode(value.access);
            const id = swift.memory.retain(bytes);
            i32Stack.push(bytes.length);
            i32Stack.push(id);
            const bytes1 = textEncoder.encode(value.format);
            const id1 = swift.memory.retain(bytes1);
            i32Stack.push(bytes1.length);
            i32Stack.push(id1);
            const bytes2 = textEncoder.encode(value.viewDimension);
            const id2 = swift.memory.retain(bytes2);
            i32Stack.push(bytes2.length);
            i32Stack.push(id2);
        },
        lift: () => {
            const rawValue = strStack.pop();
            const rawValue1 = strStack.pop();
            const rawValue2 = strStack.pop();
            return { access: rawValue2, format: rawValue1, viewDimension: rawValue };
        }
    });
    const __bjs_createGPUBindGroupLayoutEntryHelpers = () => ({
        lower: (value) => {
            i32Stack.push((value.binding | 0));
            i32Stack.push((value.visibility | 0));
            const isSome = value.buffer != null ? 1 : 0;
            if (isSome) {
                structHelpers.GPUBufferBindingLayout.lower(value.buffer);
            }
            i32Stack.push(isSome);
            const isSome1 = value.sampler != null ? 1 : 0;
            if (isSome1) {
                structHelpers.GPUSamplerBindingLayout.lower(value.sampler);
            }
            i32Stack.push(isSome1);
            const isSome2 = value.texture != null ? 1 : 0;
            if (isSome2) {
                structHelpers.GPUTextureBindingLayout.lower(value.texture);
            }
            i32Stack.push(isSome2);
            const isSome3 = value.storageTexture != null ? 1 : 0;
            if (isSome3) {
                structHelpers.GPUStorageTextureBindingLayout.lower(value.storageTexture);
            }
            i32Stack.push(isSome3);
        },
        lift: () => {
            const isSome = i32Stack.pop();
            let optValue;
            if (isSome === 0) {
                optValue = null;
            } else {
                const struct = structHelpers.GPUStorageTextureBindingLayout.lift();
                optValue = struct;
            }
            const isSome1 = i32Stack.pop();
            let optValue1;
            if (isSome1 === 0) {
                optValue1 = null;
            } else {
                const struct1 = structHelpers.GPUTextureBindingLayout.lift();
                optValue1 = struct1;
            }
            const isSome2 = i32Stack.pop();
            let optValue2;
            if (isSome2 === 0) {
                optValue2 = null;
            } else {
                const struct2 = structHelpers.GPUSamplerBindingLayout.lift();
                optValue2 = struct2;
            }
            const isSome3 = i32Stack.pop();
            let optValue3;
            if (isSome3 === 0) {
                optValue3 = null;
            } else {
                const struct3 = structHelpers.GPUBufferBindingLayout.lift();
                optValue3 = struct3;
            }
            const int = i32Stack.pop();
            const int1 = i32Stack.pop();
            return { binding: int1, visibility: int, buffer: optValue3, sampler: optValue2, texture: optValue1, storageTexture: optValue };
        }
    });
    const __bjs_createGPUBindGroupLayoutDescriptorHelpers = () => ({
        lower: (value) => {
            const isSome = value.label != null ? 1 : 0;
            if (isSome) {
                const bytes = textEncoder.encode(value.label);
                const id = swift.memory.retain(bytes);
                i32Stack.push(bytes.length);
                i32Stack.push(id);
            }
            i32Stack.push(isSome);
            for (const elem of value.entries) {
                structHelpers.GPUBindGroupLayoutEntry.lower(elem);
            }
            i32Stack.push(value.entries.length);
        },
        lift: () => {
            const arrayLen = i32Stack.pop();
            const arrayResult = [];
            for (let i = 0; i < arrayLen; i++) {
                const struct = structHelpers.GPUBindGroupLayoutEntry.lift();
                arrayResult.push(struct);
            }
            arrayResult.reverse();
            const isSome = i32Stack.pop();
            let optValue;
            if (isSome === 0) {
                optValue = null;
            } else {
                const string = strStack.pop();
                optValue = string;
            }
            return { label: optValue, entries: arrayResult };
        }
    });
    const __bjs_createGPUMultisampleStateHelpers = () => ({
        lower: (value) => {
            i32Stack.push((value.count | 0));
            i32Stack.push((value.mask | 0));
            i32Stack.push(value.alphaToCoverageEnabled ? 1 : 0);
        },
        lift: () => {
            const bool = i32Stack.pop() !== 0;
            const int = i32Stack.pop();
            const int1 = i32Stack.pop();
            return { count: int1, mask: int, alphaToCoverageEnabled: bool };
        }
    });
    const __bjs_createGPUVertexStateHelpers = () => ({
        lower: (value) => {
            let id;
            if (value.module != null) {
                id = swift.memory.retain(value.module);
            } else {
                id = undefined;
            }
            i32Stack.push(id !== undefined ? id : 0);
            const isSome = value.entryPoint != null ? 1 : 0;
            if (isSome) {
                const bytes = textEncoder.encode(value.entryPoint);
                const id1 = swift.memory.retain(bytes);
                i32Stack.push(bytes.length);
                i32Stack.push(id1);
            }
            i32Stack.push(isSome);
            for (const elem of value.buffers) {
                structHelpers.GPUVertexBufferLayout.lower(elem);
            }
            i32Stack.push(value.buffers.length);
        },
        lift: () => {
            const arrayLen = i32Stack.pop();
            const arrayResult = [];
            for (let i = 0; i < arrayLen; i++) {
                const struct = structHelpers.GPUVertexBufferLayout.lift();
                arrayResult.push(struct);
            }
            arrayResult.reverse();
            const isSome = i32Stack.pop();
            let optValue;
            if (isSome === 0) {
                optValue = null;
            } else {
                const string = strStack.pop();
                optValue = string;
            }
            const objectId = i32Stack.pop();
            let value;
            if (objectId !== 0) {
                value = swift.memory.getObject(objectId);
                swift.memory.release(objectId);
            } else {
                value = null;
            }
            return { module: value, entryPoint: optValue, buffers: arrayResult };
        }
    });
    const __bjs_createGPUFragmentStateHelpers = () => ({
        lower: (value) => {
            let id;
            if (value.module != null) {
                id = swift.memory.retain(value.module);
            } else {
                id = undefined;
            }
            i32Stack.push(id !== undefined ? id : 0);
            const isSome = value.entryPoint != null ? 1 : 0;
            if (isSome) {
                const bytes = textEncoder.encode(value.entryPoint);
                const id1 = swift.memory.retain(bytes);
                i32Stack.push(bytes.length);
                i32Stack.push(id1);
            }
            i32Stack.push(isSome);
            for (const elem of value.targets) {
                structHelpers.GPUColorTargetState.lower(elem);
            }
            i32Stack.push(value.targets.length);
        },
        lift: () => {
            const arrayLen = i32Stack.pop();
            const arrayResult = [];
            for (let i = 0; i < arrayLen; i++) {
                const struct = structHelpers.GPUColorTargetState.lift();
                arrayResult.push(struct);
            }
            arrayResult.reverse();
            const isSome = i32Stack.pop();
            let optValue;
            if (isSome === 0) {
                optValue = null;
            } else {
                const string = strStack.pop();
                optValue = string;
            }
            const objectId = i32Stack.pop();
            let value;
            if (objectId !== 0) {
                value = swift.memory.getObject(objectId);
                swift.memory.release(objectId);
            } else {
                value = null;
            }
            return { module: value, entryPoint: optValue, targets: arrayResult };
        }
    });
    const __bjs_createGPURenderPipelineDescriptorHelpers = () => ({
        lower: (value) => {
            const isSome = value.label != null ? 1 : 0;
            if (isSome) {
                const bytes = textEncoder.encode(value.label);
                const id = swift.memory.retain(bytes);
                i32Stack.push(bytes.length);
                i32Stack.push(id);
            }
            i32Stack.push(isSome);
            const isSome1 = value.layout != null ? 1 : 0;
            if (isSome1) {
                const objId = swift.memory.retain(value.layout);
                i32Stack.push(objId);
            }
            i32Stack.push(isSome1);
            structHelpers.GPUVertexState.lower(value.vertex);
            structHelpers.GPUPrimitiveState.lower(value.primitive);
            const isSome2 = value.depthStencil != null ? 1 : 0;
            if (isSome2) {
                structHelpers.GPUDepthStencilState.lower(value.depthStencil);
            }
            i32Stack.push(isSome2);
            structHelpers.GPUMultisampleState.lower(value.multisample);
            const isSome3 = value.fragment != null ? 1 : 0;
            if (isSome3) {
                structHelpers.GPUFragmentState.lower(value.fragment);
            }
            i32Stack.push(isSome3);
        },
        lift: () => {
            const isSome = i32Stack.pop();
            let optValue;
            if (isSome === 0) {
                optValue = null;
            } else {
                const struct = structHelpers.GPUFragmentState.lift();
                optValue = struct;
            }
            const struct1 = structHelpers.GPUMultisampleState.lift();
            const isSome1 = i32Stack.pop();
            let optValue1;
            if (isSome1 === 0) {
                optValue1 = null;
            } else {
                const struct2 = structHelpers.GPUDepthStencilState.lift();
                optValue1 = struct2;
            }
            const struct3 = structHelpers.GPUPrimitiveState.lift();
            const struct4 = structHelpers.GPUVertexState.lift();
            const isSome2 = i32Stack.pop();
            let optValue2;
            if (isSome2 === 0) {
                optValue2 = null;
            } else {
                const objId = i32Stack.pop();
                const obj = swift.memory.getObject(objId);
                swift.memory.release(objId);
                optValue2 = obj;
            }
            const isSome3 = i32Stack.pop();
            let optValue3;
            if (isSome3 === 0) {
                optValue3 = null;
            } else {
                const string = strStack.pop();
                optValue3 = string;
            }
            return { label: optValue3, layout: optValue2, vertex: struct4, primitive: struct3, depthStencil: optValue1, multisample: struct1, fragment: optValue };
        }
    });
    const __bjs_createGPUBufferBindingHelpers = () => ({
        lower: (value) => {
            let id;
            if (value.buffer != null) {
                id = swift.memory.retain(value.buffer);
            } else {
                id = undefined;
            }
            i32Stack.push(id !== undefined ? id : 0);
            const isSome = value.offset != null ? 1 : 0;
            if (isSome) {
                i32Stack.push((value.offset | 0));
            }
            i32Stack.push(isSome);
            const isSome1 = value.size != null ? 1 : 0;
            if (isSome1) {
                i32Stack.push((value.size | 0));
            }
            i32Stack.push(isSome1);
        },
        lift: () => {
            const isSome = i32Stack.pop();
            let optValue;
            if (isSome === 0) {
                optValue = null;
            } else {
                const int = i32Stack.pop();
                optValue = int;
            }
            const isSome1 = i32Stack.pop();
            let optValue1;
            if (isSome1 === 0) {
                optValue1 = null;
            } else {
                const int1 = i32Stack.pop();
                optValue1 = int1;
            }
            const objectId = i32Stack.pop();
            let value;
            if (objectId !== 0) {
                value = swift.memory.getObject(objectId);
                swift.memory.release(objectId);
            } else {
                value = null;
            }
            return { buffer: value, offset: optValue1, size: optValue };
        }
    });
    const __bjs_createGPUBindGroupEntryHelpers = () => ({
        lower: (value) => {
            i32Stack.push((value.binding | 0));
            structHelpers.GPUBufferBinding.lower(value.resource);
        },
        lift: () => {
            const struct = structHelpers.GPUBufferBinding.lift();
            const int = i32Stack.pop();
            return { binding: int, resource: struct };
        }
    });
    const __bjs_createGPUBindGroupDescriptorHelpers = () => ({
        lower: (value) => {
            const isSome = value.label != null ? 1 : 0;
            if (isSome) {
                const bytes = textEncoder.encode(value.label);
                const id = swift.memory.retain(bytes);
                i32Stack.push(bytes.length);
                i32Stack.push(id);
            }
            i32Stack.push(isSome);
            let id1;
            if (value.layout != null) {
                id1 = swift.memory.retain(value.layout);
            } else {
                id1 = undefined;
            }
            i32Stack.push(id1 !== undefined ? id1 : 0);
            for (const elem of value.entries) {
                structHelpers.GPUBindGroupEntry.lower(elem);
            }
            i32Stack.push(value.entries.length);
        },
        lift: () => {
            const arrayLen = i32Stack.pop();
            const arrayResult = [];
            for (let i = 0; i < arrayLen; i++) {
                const struct = structHelpers.GPUBindGroupEntry.lift();
                arrayResult.push(struct);
            }
            arrayResult.reverse();
            const objectId = i32Stack.pop();
            let value;
            if (objectId !== 0) {
                value = swift.memory.getObject(objectId);
                swift.memory.release(objectId);
            } else {
                value = null;
            }
            const isSome = i32Stack.pop();
            let optValue;
            if (isSome === 0) {
                optValue = null;
            } else {
                const string = strStack.pop();
                optValue = string;
            }
            return { label: optValue, layout: value, entries: arrayResult };
        }
    });
    const __bjs_createGPUCanvasConfigurationHelpers = () => ({
        lower: (value) => {
            let id;
            if (value.device != null) {
                id = swift.memory.retain(value.device);
            } else {
                id = undefined;
            }
            i32Stack.push(id !== undefined ? id : 0);
            const bytes = textEncoder.encode(value.format);
            const id1 = swift.memory.retain(bytes);
            i32Stack.push(bytes.length);
            i32Stack.push(id1);
            const bytes1 = textEncoder.encode(value.alphaMode);
            const id2 = swift.memory.retain(bytes1);
            i32Stack.push(bytes1.length);
            i32Stack.push(id2);
        },
        lift: () => {
            const rawValue = strStack.pop();
            const rawValue1 = strStack.pop();
            const objectId = i32Stack.pop();
            let value;
            if (objectId !== 0) {
                value = swift.memory.getObject(objectId);
                swift.memory.release(objectId);
            } else {
                value = null;
            }
            return { device: value, format: rawValue1, alphaMode: rawValue };
        }
    });
    const __bjs_createGPUComputeStateHelpers = () => ({
        lower: (value) => {
            let id;
            if (value.module != null) {
                id = swift.memory.retain(value.module);
            } else {
                id = undefined;
            }
            i32Stack.push(id !== undefined ? id : 0);
            const isSome = value.entryPoint != null ? 1 : 0;
            if (isSome) {
                const bytes = textEncoder.encode(value.entryPoint);
                const id1 = swift.memory.retain(bytes);
                i32Stack.push(bytes.length);
                i32Stack.push(id1);
            }
            i32Stack.push(isSome);
        },
        lift: () => {
            const isSome = i32Stack.pop();
            let optValue;
            if (isSome === 0) {
                optValue = null;
            } else {
                const string = strStack.pop();
                optValue = string;
            }
            const objectId = i32Stack.pop();
            let value;
            if (objectId !== 0) {
                value = swift.memory.getObject(objectId);
                swift.memory.release(objectId);
            } else {
                value = null;
            }
            return { module: value, entryPoint: optValue };
        }
    });
    const __bjs_createGPUComputePipelineDescriptorHelpers = () => ({
        lower: (value) => {
            const isSome = value.label != null ? 1 : 0;
            if (isSome) {
                const bytes = textEncoder.encode(value.label);
                const id = swift.memory.retain(bytes);
                i32Stack.push(bytes.length);
                i32Stack.push(id);
            }
            i32Stack.push(isSome);
            const isSome1 = value.layout != null ? 1 : 0;
            if (isSome1) {
                const objId = swift.memory.retain(value.layout);
                i32Stack.push(objId);
            }
            i32Stack.push(isSome1);
            structHelpers.GPUComputeState.lower(value.compute);
        },
        lift: () => {
            const struct = structHelpers.GPUComputeState.lift();
            const isSome = i32Stack.pop();
            let optValue;
            if (isSome === 0) {
                optValue = null;
            } else {
                const objId = i32Stack.pop();
                const obj = swift.memory.getObject(objId);
                swift.memory.release(objId);
                optValue = obj;
            }
            const isSome1 = i32Stack.pop();
            let optValue1;
            if (isSome1 === 0) {
                optValue1 = null;
            } else {
                const string = strStack.pop();
                optValue1 = string;
            }
            return { label: optValue1, layout: optValue, compute: struct };
        }
    });
    const __bjs_createGPUPipelineLayoutDescriptorHelpers = () => ({
        lower: (value) => {
            const isSome = value.label != null ? 1 : 0;
            if (isSome) {
                const bytes = textEncoder.encode(value.label);
                const id = swift.memory.retain(bytes);
                i32Stack.push(bytes.length);
                i32Stack.push(id);
            }
            i32Stack.push(isSome);
            for (const elem of value.bindGroupLayouts) {
                const objId = swift.memory.retain(elem);
                i32Stack.push(objId);
            }
            i32Stack.push(value.bindGroupLayouts.length);
        },
        lift: () => {
            const arrayLen = i32Stack.pop();
            const arrayResult = [];
            for (let i = 0; i < arrayLen; i++) {
                const objId = i32Stack.pop();
                const obj = swift.memory.getObject(objId);
                swift.memory.release(objId);
                arrayResult.push(obj);
            }
            arrayResult.reverse();
            const isSome = i32Stack.pop();
            let optValue;
            if (isSome === 0) {
                optValue = null;
            } else {
                const string = strStack.pop();
                optValue = string;
            }
            return { label: optValue, bindGroupLayouts: arrayResult };
        }
    });

    return {
        /**
         * @param {WebAssembly.Imports} importObject
         */
        addImports: (importObject, importsContext) => {
            bjs = {};
            importObject["bjs"] = bjs;
            bjs["swift_js_return_string"] = function(ptr, len) {
                tmpRetString = decodeString(ptr, len);
            }
            bjs["swift_js_init_memory"] = function(sourceId, bytesPtr) {
                const source = swift.memory.getObject(sourceId);
                swift.memory.release(sourceId);
                const bytes = new Uint8Array(memory.buffer, bytesPtr);
                bytes.set(source);
            }
            bjs["swift_js_make_js_string"] = function(ptr, len) {
                return swift.memory.retain(decodeString(ptr, len));
            }
            bjs["swift_js_init_memory_with_result"] = function(ptr, len) {
                const target = new Uint8Array(memory.buffer, ptr, len);
                target.set(tmpRetBytes);
                tmpRetBytes = undefined;
            }
            bjs["swift_js_throw"] = function(id) {
                tmpRetException = swift.memory.retainByRef(id);
            }
            bjs["swift_js_retain"] = function(id) {
                return swift.memory.retainByRef(id);
            }
            bjs["swift_js_release"] = function(id) {
                swift.memory.release(id);
            }
            bjs["swift_js_push_i32"] = function(v) {
                i32Stack.push(v | 0);
            }
            bjs["swift_js_push_f32"] = function(v) {
                f32Stack.push(Math.fround(v));
            }
            bjs["swift_js_push_f64"] = function(v) {
                f64Stack.push(v);
            }
            bjs["swift_js_push_string"] = function(ptr, len) {
                const value = decodeString(ptr, len);
                strStack.push(value);
            }
            bjs["swift_js_pop_i32"] = function() {
                return i32Stack.pop();
            }
            bjs["swift_js_pop_f32"] = function() {
                return f32Stack.pop();
            }
            bjs["swift_js_pop_f64"] = function() {
                return f64Stack.pop();
            }
            bjs["swift_js_push_pointer"] = function(pointer) {
                ptrStack.push(pointer);
            }
            bjs["swift_js_pop_pointer"] = function() {
                return ptrStack.pop();
            }
            bjs["swift_js_struct_lower_GPUBufferDescriptor"] = function(objectId) {
                structHelpers.GPUBufferDescriptor.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUBufferDescriptor"] = function() {
                const value = structHelpers.GPUBufferDescriptor.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUShaderModuleDescriptor"] = function(objectId) {
                structHelpers.GPUShaderModuleDescriptor.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUShaderModuleDescriptor"] = function() {
                const value = structHelpers.GPUShaderModuleDescriptor.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUVertexAttribute"] = function(objectId) {
                structHelpers.GPUVertexAttribute.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUVertexAttribute"] = function() {
                const value = structHelpers.GPUVertexAttribute.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUVertexBufferLayout"] = function(objectId) {
                structHelpers.GPUVertexBufferLayout.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUVertexBufferLayout"] = function() {
                const value = structHelpers.GPUVertexBufferLayout.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUPrimitiveState"] = function(objectId) {
                structHelpers.GPUPrimitiveState.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUPrimitiveState"] = function() {
                const value = structHelpers.GPUPrimitiveState.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUBlendComponent"] = function(objectId) {
                structHelpers.GPUBlendComponent.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUBlendComponent"] = function() {
                const value = structHelpers.GPUBlendComponent.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUBlendState"] = function(objectId) {
                structHelpers.GPUBlendState.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUBlendState"] = function() {
                const value = structHelpers.GPUBlendState.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUColorTargetState"] = function(objectId) {
                structHelpers.GPUColorTargetState.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUColorTargetState"] = function() {
                const value = structHelpers.GPUColorTargetState.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUColor"] = function(objectId) {
                structHelpers.GPUColor.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUColor"] = function() {
                const value = structHelpers.GPUColor.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUCommandEncoderDescriptor"] = function(objectId) {
                structHelpers.GPUCommandEncoderDescriptor.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUCommandEncoderDescriptor"] = function() {
                const value = structHelpers.GPUCommandEncoderDescriptor.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUCommandBufferDescriptor"] = function(objectId) {
                structHelpers.GPUCommandBufferDescriptor.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUCommandBufferDescriptor"] = function() {
                const value = structHelpers.GPUCommandBufferDescriptor.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPURenderPassColorAttachment"] = function(objectId) {
                structHelpers.GPURenderPassColorAttachment.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPURenderPassColorAttachment"] = function() {
                const value = structHelpers.GPURenderPassColorAttachment.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPURenderPassDepthStencilAttachment"] = function(objectId) {
                structHelpers.GPURenderPassDepthStencilAttachment.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPURenderPassDepthStencilAttachment"] = function() {
                const value = structHelpers.GPURenderPassDepthStencilAttachment.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPURenderPassDescriptor"] = function(objectId) {
                structHelpers.GPURenderPassDescriptor.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPURenderPassDescriptor"] = function() {
                const value = structHelpers.GPURenderPassDescriptor.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUComputePassDescriptor"] = function(objectId) {
                structHelpers.GPUComputePassDescriptor.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUComputePassDescriptor"] = function() {
                const value = structHelpers.GPUComputePassDescriptor.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUStencilFaceState"] = function(objectId) {
                structHelpers.GPUStencilFaceState.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUStencilFaceState"] = function() {
                const value = structHelpers.GPUStencilFaceState.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUDepthStencilState"] = function(objectId) {
                structHelpers.GPUDepthStencilState.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUDepthStencilState"] = function() {
                const value = structHelpers.GPUDepthStencilState.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUExtent3D"] = function(objectId) {
                structHelpers.GPUExtent3D.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUExtent3D"] = function() {
                const value = structHelpers.GPUExtent3D.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUOrigin3D"] = function(objectId) {
                structHelpers.GPUOrigin3D.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUOrigin3D"] = function() {
                const value = structHelpers.GPUOrigin3D.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUTextureDescriptor"] = function(objectId) {
                structHelpers.GPUTextureDescriptor.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUTextureDescriptor"] = function() {
                const value = structHelpers.GPUTextureDescriptor.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUTextureViewDescriptor"] = function(objectId) {
                structHelpers.GPUTextureViewDescriptor.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUTextureViewDescriptor"] = function() {
                const value = structHelpers.GPUTextureViewDescriptor.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUSamplerDescriptor"] = function(objectId) {
                structHelpers.GPUSamplerDescriptor.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUSamplerDescriptor"] = function() {
                const value = structHelpers.GPUSamplerDescriptor.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUQuerySetDescriptor"] = function(objectId) {
                structHelpers.GPUQuerySetDescriptor.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUQuerySetDescriptor"] = function() {
                const value = structHelpers.GPUQuerySetDescriptor.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUPassTimestampWrites"] = function(objectId) {
                structHelpers.GPUPassTimestampWrites.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUPassTimestampWrites"] = function() {
                const value = structHelpers.GPUPassTimestampWrites.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUBufferBindingLayout"] = function(objectId) {
                structHelpers.GPUBufferBindingLayout.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUBufferBindingLayout"] = function() {
                const value = structHelpers.GPUBufferBindingLayout.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUSamplerBindingLayout"] = function(objectId) {
                structHelpers.GPUSamplerBindingLayout.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUSamplerBindingLayout"] = function() {
                const value = structHelpers.GPUSamplerBindingLayout.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUTextureBindingLayout"] = function(objectId) {
                structHelpers.GPUTextureBindingLayout.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUTextureBindingLayout"] = function() {
                const value = structHelpers.GPUTextureBindingLayout.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUStorageTextureBindingLayout"] = function(objectId) {
                structHelpers.GPUStorageTextureBindingLayout.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUStorageTextureBindingLayout"] = function() {
                const value = structHelpers.GPUStorageTextureBindingLayout.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUBindGroupLayoutEntry"] = function(objectId) {
                structHelpers.GPUBindGroupLayoutEntry.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUBindGroupLayoutEntry"] = function() {
                const value = structHelpers.GPUBindGroupLayoutEntry.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUBindGroupLayoutDescriptor"] = function(objectId) {
                structHelpers.GPUBindGroupLayoutDescriptor.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUBindGroupLayoutDescriptor"] = function() {
                const value = structHelpers.GPUBindGroupLayoutDescriptor.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUMultisampleState"] = function(objectId) {
                structHelpers.GPUMultisampleState.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUMultisampleState"] = function() {
                const value = structHelpers.GPUMultisampleState.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUVertexState"] = function(objectId) {
                structHelpers.GPUVertexState.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUVertexState"] = function() {
                const value = structHelpers.GPUVertexState.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUFragmentState"] = function(objectId) {
                structHelpers.GPUFragmentState.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUFragmentState"] = function() {
                const value = structHelpers.GPUFragmentState.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPURenderPipelineDescriptor"] = function(objectId) {
                structHelpers.GPURenderPipelineDescriptor.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPURenderPipelineDescriptor"] = function() {
                const value = structHelpers.GPURenderPipelineDescriptor.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUBufferBinding"] = function(objectId) {
                structHelpers.GPUBufferBinding.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUBufferBinding"] = function() {
                const value = structHelpers.GPUBufferBinding.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUBindGroupEntry"] = function(objectId) {
                structHelpers.GPUBindGroupEntry.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUBindGroupEntry"] = function() {
                const value = structHelpers.GPUBindGroupEntry.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUBindGroupDescriptor"] = function(objectId) {
                structHelpers.GPUBindGroupDescriptor.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUBindGroupDescriptor"] = function() {
                const value = structHelpers.GPUBindGroupDescriptor.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUCanvasConfiguration"] = function(objectId) {
                structHelpers.GPUCanvasConfiguration.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUCanvasConfiguration"] = function() {
                const value = structHelpers.GPUCanvasConfiguration.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUComputeState"] = function(objectId) {
                structHelpers.GPUComputeState.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUComputeState"] = function() {
                const value = structHelpers.GPUComputeState.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUComputePipelineDescriptor"] = function(objectId) {
                structHelpers.GPUComputePipelineDescriptor.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUComputePipelineDescriptor"] = function() {
                const value = structHelpers.GPUComputePipelineDescriptor.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_struct_lower_GPUPipelineLayoutDescriptor"] = function(objectId) {
                structHelpers.GPUPipelineLayoutDescriptor.lower(swift.memory.getObject(objectId));
            }
            bjs["swift_js_struct_lift_GPUPipelineLayoutDescriptor"] = function() {
                const value = structHelpers.GPUPipelineLayoutDescriptor.lift();
                return swift.memory.retain(value);
            }
            bjs["swift_js_return_optional_bool"] = function(isSome, value) {
                if (isSome === 0) {
                    tmpRetOptionalBool = null;
                } else {
                    tmpRetOptionalBool = value !== 0;
                }
            }
            bjs["swift_js_return_optional_int"] = function(isSome, value) {
                if (isSome === 0) {
                    tmpRetOptionalInt = null;
                } else {
                    tmpRetOptionalInt = value | 0;
                }
            }
            bjs["swift_js_return_optional_float"] = function(isSome, value) {
                if (isSome === 0) {
                    tmpRetOptionalFloat = null;
                } else {
                    tmpRetOptionalFloat = Math.fround(value);
                }
            }
            bjs["swift_js_return_optional_double"] = function(isSome, value) {
                if (isSome === 0) {
                    tmpRetOptionalDouble = null;
                } else {
                    tmpRetOptionalDouble = value;
                }
            }
            bjs["swift_js_return_optional_string"] = function(isSome, ptr, len) {
                if (isSome === 0) {
                    tmpRetString = null;
                } else {
                    tmpRetString = decodeString(ptr, len);
                }
            }
            bjs["swift_js_return_optional_object"] = function(isSome, objectId) {
                if (isSome === 0) {
                    tmpRetString = null;
                } else {
                    tmpRetString = swift.memory.getObject(objectId);
                }
            }
            bjs["swift_js_return_optional_heap_object"] = function(isSome, pointer) {
                if (isSome === 0) {
                    tmpRetOptionalHeapObject = null;
                } else {
                    tmpRetOptionalHeapObject = pointer;
                }
            }
            bjs["swift_js_get_optional_int_presence"] = function() {
                return tmpRetOptionalInt != null ? 1 : 0;
            }
            bjs["swift_js_get_optional_int_value"] = function() {
                const value = tmpRetOptionalInt;
                tmpRetOptionalInt = undefined;
                return value;
            }
            bjs["swift_js_get_optional_string"] = function() {
                const str = tmpRetString;
                tmpRetString = undefined;
                if (str == null) {
                    return -1;
                } else {
                    const bytes = textEncoder.encode(str);
                    tmpRetBytes = bytes;
                    return bytes.length;
                }
            }
            bjs["swift_js_get_optional_float_presence"] = function() {
                return tmpRetOptionalFloat != null ? 1 : 0;
            }
            bjs["swift_js_get_optional_float_value"] = function() {
                const value = tmpRetOptionalFloat;
                tmpRetOptionalFloat = undefined;
                return value;
            }
            bjs["swift_js_get_optional_double_presence"] = function() {
                return tmpRetOptionalDouble != null ? 1 : 0;
            }
            bjs["swift_js_get_optional_double_value"] = function() {
                const value = tmpRetOptionalDouble;
                tmpRetOptionalDouble = undefined;
                return value;
            }
            bjs["swift_js_get_optional_heap_object_pointer"] = function() {
                const pointer = tmpRetOptionalHeapObject;
                tmpRetOptionalHeapObject = undefined;
                return pointer || 0;
            }
            bjs["swift_js_closure_unregister"] = function(funcRef) {}
            const WebGPUWasm = importObject["WebGPUWasm"] = importObject["WebGPUWasm"] || {};
            WebGPUWasm["bjs_GPUAdapter_label__set"] = function bjs_GPUAdapter_label__set(self, newValueBytes, newValueCount) {
                try {
                    const string = decodeString(newValueBytes, newValueCount);
                    swift.memory.getObject(self).label = string;
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUAdapter__hasFeature"] = function bjs_GPUAdapter__hasFeature(self, featureBytes, featureCount) {
                try {
                    const string = decodeString(featureBytes, featureCount);
                    let ret = swift.memory.getObject(self).hasFeature(string);
                    return ret ? 1 : 0;
                } catch (error) {
                    setException(error);
                    return 0
                }
            }
            WebGPUWasm["bjs_GPUBindGroup_label__set"] = function bjs_GPUBindGroup_label__set(self, newValueBytes, newValueCount) {
                try {
                    const string = decodeString(newValueBytes, newValueCount);
                    swift.memory.getObject(self).label = string;
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUBindGroupLayout_label__set"] = function bjs_GPUBindGroupLayout_label__set(self, newValueBytes, newValueCount) {
                try {
                    const string = decodeString(newValueBytes, newValueCount);
                    swift.memory.getObject(self).label = string;
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUBuffer__size_get"] = function bjs_GPUBuffer__size_get(self) {
                try {
                    let ret = swift.memory.getObject(self).size;
                    return ret;
                } catch (error) {
                    setException(error);
                    return 0
                }
            }
            WebGPUWasm["bjs_GPUBuffer__usage_get"] = function bjs_GPUBuffer__usage_get(self) {
                try {
                    let ret = swift.memory.getObject(self).usage;
                    return ret;
                } catch (error) {
                    setException(error);
                    return 0
                }
            }
            WebGPUWasm["bjs_GPUBuffer__mapState_get"] = function bjs_GPUBuffer__mapState_get(self) {
                try {
                    let ret = swift.memory.getObject(self).mapState;
                    tmpRetBytes = textEncoder.encode(ret);
                    return tmpRetBytes.length;
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUBuffer_label__set"] = function bjs_GPUBuffer_label__set(self, newValueBytes, newValueCount) {
                try {
                    const string = decodeString(newValueBytes, newValueCount);
                    swift.memory.getObject(self).label = string;
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUBuffer__destroy"] = function bjs_GPUBuffer__destroy(self) {
                try {
                    swift.memory.getObject(self).destroy();
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUBuffer__mapAsync"] = function bjs_GPUBuffer__mapAsync(self, mode, offset, size) {
                try {
                    let ret = swift.memory.getObject(self).mapAsync(mode, offset, size);
                    return swift.memory.retain(ret);
                } catch (error) {
                    setException(error);
                    return 0
                }
            }
            WebGPUWasm["bjs_GPUBuffer__getMappedRange"] = function bjs_GPUBuffer__getMappedRange(self, offset, size) {
                try {
                    let ret = swift.memory.getObject(self).getMappedRange(offset, size);
                    return swift.memory.retain(ret);
                } catch (error) {
                    setException(error);
                    return 0
                }
            }
            WebGPUWasm["bjs_GPUBuffer__unmap"] = function bjs_GPUBuffer__unmap(self) {
                try {
                    swift.memory.getObject(self).unmap();
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUCanvasContext__configure"] = function bjs_GPUCanvasContext__configure(self, configuration) {
                try {
                    const value = swift.memory.getObject(configuration);
                    swift.memory.release(configuration);
                    swift.memory.getObject(self).configure(value);
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUCanvasContext__getCurrentTexture"] = function bjs_GPUCanvasContext__getCurrentTexture(self) {
                try {
                    let ret = swift.memory.getObject(self).getCurrentTexture();
                    return swift.memory.retain(ret);
                } catch (error) {
                    setException(error);
                    return 0
                }
            }
            WebGPUWasm["bjs_GPUCommandBuffer_label__set"] = function bjs_GPUCommandBuffer_label__set(self, newValueBytes, newValueCount) {
                try {
                    const string = decodeString(newValueBytes, newValueCount);
                    swift.memory.getObject(self).label = string;
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUCommandEncoder_label__set"] = function bjs_GPUCommandEncoder_label__set(self, newValueBytes, newValueCount) {
                try {
                    const string = decodeString(newValueBytes, newValueCount);
                    swift.memory.getObject(self).label = string;
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUCommandEncoder__finish"] = function bjs_GPUCommandEncoder__finish(self, descriptor) {
                try {
                    const value = swift.memory.getObject(descriptor);
                    swift.memory.release(descriptor);
                    let ret = swift.memory.getObject(self).finish(value);
                    return swift.memory.retain(ret);
                } catch (error) {
                    setException(error);
                    return 0
                }
            }
            WebGPUWasm["bjs_GPUCommandEncoder__copyBufferToBuffer"] = function bjs_GPUCommandEncoder__copyBufferToBuffer(self, source, sourceOffset, destination, destinationOffset, size) {
                try {
                    swift.memory.getObject(self).copyBufferToBuffer(swift.memory.getObject(source), sourceOffset, swift.memory.getObject(destination), destinationOffset, size);
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUCommandEncoder__copyBufferToTexture"] = function bjs_GPUCommandEncoder__copyBufferToTexture(self, source, destination, copySize) {
                try {
                    swift.memory.getObject(self).copyBufferToTexture(swift.memory.getObject(source), swift.memory.getObject(destination), swift.memory.getObject(copySize));
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUCommandEncoder__copyTextureToBuffer"] = function bjs_GPUCommandEncoder__copyTextureToBuffer(self, source, destination, copySize) {
                try {
                    swift.memory.getObject(self).copyTextureToBuffer(swift.memory.getObject(source), swift.memory.getObject(destination), swift.memory.getObject(copySize));
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUCommandEncoder__resolveQuerySet"] = function bjs_GPUCommandEncoder__resolveQuerySet(self, querySet, firstQuery, queryCount, destination, destinationOffset) {
                try {
                    swift.memory.getObject(self).resolveQuerySet(swift.memory.getObject(querySet), firstQuery, queryCount, swift.memory.getObject(destination), destinationOffset);
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUCommandEncoder__clearBuffer"] = function bjs_GPUCommandEncoder__clearBuffer(self, buffer, offset) {
                try {
                    swift.memory.getObject(self).clearBuffer(swift.memory.getObject(buffer), offset);
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUCommandEncoder__clearBufferWithSize"] = function bjs_GPUCommandEncoder__clearBufferWithSize(self, buffer, offset, size) {
                try {
                    swift.memory.getObject(self).clearBuffer(swift.memory.getObject(buffer), offset, size);
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUCommandEncoder__writeTimestamp"] = function bjs_GPUCommandEncoder__writeTimestamp(self, querySet, queryIndex) {
                try {
                    swift.memory.getObject(self).writeTimestamp(swift.memory.getObject(querySet), queryIndex);
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUComputePassEncoder_label__set"] = function bjs_GPUComputePassEncoder_label__set(self, newValueBytes, newValueCount) {
                try {
                    const string = decodeString(newValueBytes, newValueCount);
                    swift.memory.getObject(self).label = string;
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUComputePassEncoder__setPipeline"] = function bjs_GPUComputePassEncoder__setPipeline(self, pipeline) {
                try {
                    swift.memory.getObject(self).setPipeline(swift.memory.getObject(pipeline));
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUComputePassEncoder__setBindGroup"] = function bjs_GPUComputePassEncoder__setBindGroup(self, groupIndex, group) {
                try {
                    swift.memory.getObject(self).setBindGroup(groupIndex, swift.memory.getObject(group));
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUComputePassEncoder__setBindGroupWithOffsets"] = function bjs_GPUComputePassEncoder__setBindGroupWithOffsets(self, groupIndex, group, dynamicOffsets) {
                try {
                    swift.memory.getObject(self).setBindGroup(groupIndex, swift.memory.getObject(group), swift.memory.getObject(dynamicOffsets));
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUComputePassEncoder__dispatchWorkgroups"] = function bjs_GPUComputePassEncoder__dispatchWorkgroups(self, workgroupCountX, workgroupCountY, workgroupCountZ) {
                try {
                    swift.memory.getObject(self).dispatchWorkgroups(workgroupCountX, workgroupCountY, workgroupCountZ);
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUComputePassEncoder__dispatchWorkgroupsIndirect"] = function bjs_GPUComputePassEncoder__dispatchWorkgroupsIndirect(self, indirectBuffer, indirectOffset) {
                try {
                    swift.memory.getObject(self).dispatchWorkgroupsIndirect(swift.memory.getObject(indirectBuffer), indirectOffset);
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUComputePassEncoder__writeTimestamp"] = function bjs_GPUComputePassEncoder__writeTimestamp(self, querySet, queryIndex) {
                try {
                    swift.memory.getObject(self).writeTimestamp(swift.memory.getObject(querySet), queryIndex);
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUComputePassEncoder__end"] = function bjs_GPUComputePassEncoder__end(self) {
                try {
                    swift.memory.getObject(self).end();
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUComputePipeline_label__set"] = function bjs_GPUComputePipeline_label__set(self, newValueBytes, newValueCount) {
                try {
                    const string = decodeString(newValueBytes, newValueCount);
                    swift.memory.getObject(self).label = string;
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUDevice__queue_get"] = function bjs_GPUDevice__queue_get(self) {
                try {
                    let ret = swift.memory.getObject(self).queue;
                    return swift.memory.retain(ret);
                } catch (error) {
                    setException(error);
                    return 0
                }
            }
            WebGPUWasm["bjs_GPUDevice_label__set"] = function bjs_GPUDevice_label__set(self, newValueBytes, newValueCount) {
                try {
                    const string = decodeString(newValueBytes, newValueCount);
                    swift.memory.getObject(self).label = string;
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUDevice__createBuffer"] = function bjs_GPUDevice__createBuffer(self, descriptor) {
                try {
                    const value = swift.memory.getObject(descriptor);
                    swift.memory.release(descriptor);
                    let ret = swift.memory.getObject(self).createBuffer(value);
                    return swift.memory.retain(ret);
                } catch (error) {
                    setException(error);
                    return 0
                }
            }
            WebGPUWasm["bjs_GPUDevice__createTexture"] = function bjs_GPUDevice__createTexture(self, descriptor) {
                try {
                    const value = swift.memory.getObject(descriptor);
                    swift.memory.release(descriptor);
                    let ret = swift.memory.getObject(self).createTexture(value);
                    return swift.memory.retain(ret);
                } catch (error) {
                    setException(error);
                    return 0
                }
            }
            WebGPUWasm["bjs_GPUDevice__createQuerySet"] = function bjs_GPUDevice__createQuerySet(self, descriptor) {
                try {
                    const value = swift.memory.getObject(descriptor);
                    swift.memory.release(descriptor);
                    let ret = swift.memory.getObject(self).createQuerySet(value);
                    return swift.memory.retain(ret);
                } catch (error) {
                    setException(error);
                    return 0
                }
            }
            WebGPUWasm["bjs_GPUDevice__createShaderModule"] = function bjs_GPUDevice__createShaderModule(self, descriptor) {
                try {
                    const value = swift.memory.getObject(descriptor);
                    swift.memory.release(descriptor);
                    let ret = swift.memory.getObject(self).createShaderModule(value);
                    return swift.memory.retain(ret);
                } catch (error) {
                    setException(error);
                    return 0
                }
            }
            WebGPUWasm["bjs_GPUDevice__createCommandEncoder"] = function bjs_GPUDevice__createCommandEncoder(self, descriptor) {
                try {
                    const value = swift.memory.getObject(descriptor);
                    swift.memory.release(descriptor);
                    let ret = swift.memory.getObject(self).createCommandEncoder(value);
                    return swift.memory.retain(ret);
                } catch (error) {
                    setException(error);
                    return 0
                }
            }
            WebGPUWasm["bjs_GPUDevice__createBindGroupRaw"] = function bjs_GPUDevice__createBindGroupRaw(self, descriptor) {
                try {
                    let ret = swift.memory.getObject(self).createBindGroup(swift.memory.getObject(descriptor));
                    return swift.memory.retain(ret);
                } catch (error) {
                    setException(error);
                    return 0
                }
            }
            WebGPUWasm["bjs_GPUDevice__createPipelineLayout"] = function bjs_GPUDevice__createPipelineLayout(self, descriptor) {
                try {
                    const value = swift.memory.getObject(descriptor);
                    swift.memory.release(descriptor);
                    let ret = swift.memory.getObject(self).createPipelineLayout(value);
                    return swift.memory.retain(ret);
                } catch (error) {
                    setException(error);
                    return 0
                }
            }
            WebGPUWasm["bjs_GPUDevice__createComputePipeline"] = function bjs_GPUDevice__createComputePipeline(self, descriptor) {
                try {
                    const value = swift.memory.getObject(descriptor);
                    swift.memory.release(descriptor);
                    let ret = swift.memory.getObject(self).createComputePipeline(value);
                    return swift.memory.retain(ret);
                } catch (error) {
                    setException(error);
                    return 0
                }
            }
            WebGPUWasm["bjs_GPUDevice__pushErrorScope"] = function bjs_GPUDevice__pushErrorScope(self, filterBytes, filterCount) {
                try {
                    const string = decodeString(filterBytes, filterCount);
                    swift.memory.getObject(self).pushErrorScope(string);
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUDevice__popErrorScope"] = function bjs_GPUDevice__popErrorScope(self) {
                try {
                    let ret = swift.memory.getObject(self).popErrorScope();
                    return swift.memory.retain(ret);
                } catch (error) {
                    setException(error);
                    return 0
                }
            }
            WebGPUWasm["bjs_GPUPipelineLayout_label__set"] = function bjs_GPUPipelineLayout_label__set(self, newValueBytes, newValueCount) {
                try {
                    const string = decodeString(newValueBytes, newValueCount);
                    swift.memory.getObject(self).label = string;
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUQuerySet__type_get"] = function bjs_GPUQuerySet__type_get(self) {
                try {
                    let ret = swift.memory.getObject(self).type;
                    tmpRetBytes = textEncoder.encode(ret);
                    return tmpRetBytes.length;
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUQuerySet__count_get"] = function bjs_GPUQuerySet__count_get(self) {
                try {
                    let ret = swift.memory.getObject(self).count;
                    return ret;
                } catch (error) {
                    setException(error);
                    return 0
                }
            }
            WebGPUWasm["bjs_GPUQuerySet_label__set"] = function bjs_GPUQuerySet_label__set(self, newValueBytes, newValueCount) {
                try {
                    const string = decodeString(newValueBytes, newValueCount);
                    swift.memory.getObject(self).label = string;
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUQuerySet__destroy"] = function bjs_GPUQuerySet__destroy(self) {
                try {
                    swift.memory.getObject(self).destroy();
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUQueue_label__set"] = function bjs_GPUQueue_label__set(self, newValueBytes, newValueCount) {
                try {
                    const string = decodeString(newValueBytes, newValueCount);
                    swift.memory.getObject(self).label = string;
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUQueue__submit"] = function bjs_GPUQueue__submit(self, commandBuffers) {
                try {
                    swift.memory.getObject(self).submit(swift.memory.getObject(commandBuffers));
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUQueue__writeBuffer"] = function bjs_GPUQueue__writeBuffer(self, buffer, bufferOffset, data) {
                try {
                    swift.memory.getObject(self).writeBuffer(swift.memory.getObject(buffer), bufferOffset, swift.memory.getObject(data));
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUQueue__writeTexture"] = function bjs_GPUQueue__writeTexture(self, destination, data, dataLayout, size) {
                try {
                    swift.memory.getObject(self).writeTexture(swift.memory.getObject(destination), swift.memory.getObject(data), swift.memory.getObject(dataLayout), swift.memory.getObject(size));
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPURenderPassEncoder_label__set"] = function bjs_GPURenderPassEncoder_label__set(self, newValueBytes, newValueCount) {
                try {
                    const string = decodeString(newValueBytes, newValueCount);
                    swift.memory.getObject(self).label = string;
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPURenderPassEncoder__setPipeline"] = function bjs_GPURenderPassEncoder__setPipeline(self, pipeline) {
                try {
                    swift.memory.getObject(self).setPipeline(swift.memory.getObject(pipeline));
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPURenderPassEncoder__setVertexBuffer"] = function bjs_GPURenderPassEncoder__setVertexBuffer(self, slot, buffer, offset, size) {
                try {
                    swift.memory.getObject(self).setVertexBuffer(slot, swift.memory.getObject(buffer), offset, size);
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPURenderPassEncoder__setIndexBuffer"] = function bjs_GPURenderPassEncoder__setIndexBuffer(self, buffer, indexFormatBytes, indexFormatCount, offset, size) {
                try {
                    const string = decodeString(indexFormatBytes, indexFormatCount);
                    swift.memory.getObject(self).setIndexBuffer(swift.memory.getObject(buffer), string, offset, size);
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPURenderPassEncoder__draw"] = function bjs_GPURenderPassEncoder__draw(self, vertexCount, instanceCount, firstVertex, firstInstance) {
                try {
                    swift.memory.getObject(self).draw(vertexCount, instanceCount, firstVertex, firstInstance);
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPURenderPassEncoder__drawIndexed"] = function bjs_GPURenderPassEncoder__drawIndexed(self, indexCount, instanceCount, firstIndex, baseVertex, firstInstance) {
                try {
                    swift.memory.getObject(self).drawIndexed(indexCount, instanceCount, firstIndex, baseVertex, firstInstance);
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPURenderPassEncoder__setBindGroup"] = function bjs_GPURenderPassEncoder__setBindGroup(self, groupIndex, group, dynamicOffsets) {
                try {
                    swift.memory.getObject(self).setBindGroup(groupIndex, swift.memory.getObject(group), swift.memory.getObject(dynamicOffsets));
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPURenderPassEncoder__setScissorRect"] = function bjs_GPURenderPassEncoder__setScissorRect(self, x, y, width, height) {
                try {
                    swift.memory.getObject(self).setScissorRect(x, y, width, height);
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPURenderPassEncoder__writeTimestamp"] = function bjs_GPURenderPassEncoder__writeTimestamp(self, querySet, queryIndex) {
                try {
                    swift.memory.getObject(self).writeTimestamp(swift.memory.getObject(querySet), queryIndex);
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPURenderPassEncoder__end"] = function bjs_GPURenderPassEncoder__end(self) {
                try {
                    swift.memory.getObject(self).end();
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPURenderPipeline_label__set"] = function bjs_GPURenderPipeline_label__set(self, newValueBytes, newValueCount) {
                try {
                    const string = decodeString(newValueBytes, newValueCount);
                    swift.memory.getObject(self).label = string;
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPURenderPipeline__getBindGroupLayout"] = function bjs_GPURenderPipeline__getBindGroupLayout(self, index) {
                try {
                    let ret = swift.memory.getObject(self).getBindGroupLayout(index);
                    return swift.memory.retain(ret);
                } catch (error) {
                    setException(error);
                    return 0
                }
            }
            WebGPUWasm["bjs_GPUSampler_label__set"] = function bjs_GPUSampler_label__set(self, newValueBytes, newValueCount) {
                try {
                    const string = decodeString(newValueBytes, newValueCount);
                    swift.memory.getObject(self).label = string;
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUShaderModule_label__set"] = function bjs_GPUShaderModule_label__set(self, newValueBytes, newValueCount) {
                try {
                    const string = decodeString(newValueBytes, newValueCount);
                    swift.memory.getObject(self).label = string;
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUTexture__format_get"] = function bjs_GPUTexture__format_get(self) {
                try {
                    let ret = swift.memory.getObject(self).format;
                    tmpRetBytes = textEncoder.encode(ret);
                    return tmpRetBytes.length;
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUTexture__width_get"] = function bjs_GPUTexture__width_get(self) {
                try {
                    let ret = swift.memory.getObject(self).width;
                    return ret;
                } catch (error) {
                    setException(error);
                    return 0
                }
            }
            WebGPUWasm["bjs_GPUTexture__height_get"] = function bjs_GPUTexture__height_get(self) {
                try {
                    let ret = swift.memory.getObject(self).height;
                    return ret;
                } catch (error) {
                    setException(error);
                    return 0
                }
            }
            WebGPUWasm["bjs_GPUTexture__depthOrArrayLayers_get"] = function bjs_GPUTexture__depthOrArrayLayers_get(self) {
                try {
                    let ret = swift.memory.getObject(self).depthOrArrayLayers;
                    return ret;
                } catch (error) {
                    setException(error);
                    return 0
                }
            }
            WebGPUWasm["bjs_GPUTexture_label__set"] = function bjs_GPUTexture_label__set(self, newValueBytes, newValueCount) {
                try {
                    const string = decodeString(newValueBytes, newValueCount);
                    swift.memory.getObject(self).label = string;
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUTexture__createView"] = function bjs_GPUTexture__createView(self) {
                try {
                    let ret = swift.memory.getObject(self).createView();
                    return swift.memory.retain(ret);
                } catch (error) {
                    setException(error);
                    return 0
                }
            }
            WebGPUWasm["bjs_GPUTexture__createViewWithDescriptor"] = function bjs_GPUTexture__createViewWithDescriptor(self, descriptor) {
                try {
                    const value = swift.memory.getObject(descriptor);
                    swift.memory.release(descriptor);
                    let ret = swift.memory.getObject(self).createView(value);
                    return swift.memory.retain(ret);
                } catch (error) {
                    setException(error);
                    return 0
                }
            }
            WebGPUWasm["bjs_GPUTexture__destroy"] = function bjs_GPUTexture__destroy(self) {
                try {
                    swift.memory.getObject(self).destroy();
                } catch (error) {
                    setException(error);
                }
            }
            WebGPUWasm["bjs_GPUTextureView_label__set"] = function bjs_GPUTextureView_label__set(self, newValueBytes, newValueCount) {
                try {
                    const string = decodeString(newValueBytes, newValueCount);
                    swift.memory.getObject(self).label = string;
                } catch (error) {
                    setException(error);
                }
            }
        },
        setInstance: (i) => {
            instance = i;
            memory = instance.exports.memory;

            decodeString = (ptr, len) => { const bytes = new Uint8Array(memory.buffer, ptr >>> 0, len >>> 0); return textDecoder.decode(bytes); }

            setException = (error) => {
                instance.exports._swift_js_exception.value = swift.memory.retain(error)
            }
        },
        /** @param {WebAssembly.Instance} instance */
        createExports: (instance) => {
            const js = swift.memory.heap;
            const GPUBufferDescriptorHelpers = __bjs_createGPUBufferDescriptorHelpers();
            structHelpers.GPUBufferDescriptor = GPUBufferDescriptorHelpers;

            const GPUShaderModuleDescriptorHelpers = __bjs_createGPUShaderModuleDescriptorHelpers();
            structHelpers.GPUShaderModuleDescriptor = GPUShaderModuleDescriptorHelpers;

            const GPUVertexAttributeHelpers = __bjs_createGPUVertexAttributeHelpers();
            structHelpers.GPUVertexAttribute = GPUVertexAttributeHelpers;

            const GPUVertexBufferLayoutHelpers = __bjs_createGPUVertexBufferLayoutHelpers();
            structHelpers.GPUVertexBufferLayout = GPUVertexBufferLayoutHelpers;

            const GPUPrimitiveStateHelpers = __bjs_createGPUPrimitiveStateHelpers();
            structHelpers.GPUPrimitiveState = GPUPrimitiveStateHelpers;

            const GPUBlendComponentHelpers = __bjs_createGPUBlendComponentHelpers();
            structHelpers.GPUBlendComponent = GPUBlendComponentHelpers;

            const GPUBlendStateHelpers = __bjs_createGPUBlendStateHelpers();
            structHelpers.GPUBlendState = GPUBlendStateHelpers;

            const GPUColorTargetStateHelpers = __bjs_createGPUColorTargetStateHelpers();
            structHelpers.GPUColorTargetState = GPUColorTargetStateHelpers;

            const GPUColorHelpers = __bjs_createGPUColorHelpers();
            structHelpers.GPUColor = GPUColorHelpers;

            const GPUCommandEncoderDescriptorHelpers = __bjs_createGPUCommandEncoderDescriptorHelpers();
            structHelpers.GPUCommandEncoderDescriptor = GPUCommandEncoderDescriptorHelpers;

            const GPUCommandBufferDescriptorHelpers = __bjs_createGPUCommandBufferDescriptorHelpers();
            structHelpers.GPUCommandBufferDescriptor = GPUCommandBufferDescriptorHelpers;

            const GPURenderPassColorAttachmentHelpers = __bjs_createGPURenderPassColorAttachmentHelpers();
            structHelpers.GPURenderPassColorAttachment = GPURenderPassColorAttachmentHelpers;

            const GPURenderPassDepthStencilAttachmentHelpers = __bjs_createGPURenderPassDepthStencilAttachmentHelpers();
            structHelpers.GPURenderPassDepthStencilAttachment = GPURenderPassDepthStencilAttachmentHelpers;

            const GPURenderPassDescriptorHelpers = __bjs_createGPURenderPassDescriptorHelpers();
            structHelpers.GPURenderPassDescriptor = GPURenderPassDescriptorHelpers;

            const GPUComputePassDescriptorHelpers = __bjs_createGPUComputePassDescriptorHelpers();
            structHelpers.GPUComputePassDescriptor = GPUComputePassDescriptorHelpers;

            const GPUStencilFaceStateHelpers = __bjs_createGPUStencilFaceStateHelpers();
            structHelpers.GPUStencilFaceState = GPUStencilFaceStateHelpers;

            const GPUDepthStencilStateHelpers = __bjs_createGPUDepthStencilStateHelpers();
            structHelpers.GPUDepthStencilState = GPUDepthStencilStateHelpers;

            const GPUExtent3DHelpers = __bjs_createGPUExtent3DHelpers();
            structHelpers.GPUExtent3D = GPUExtent3DHelpers;

            const GPUOrigin3DHelpers = __bjs_createGPUOrigin3DHelpers();
            structHelpers.GPUOrigin3D = GPUOrigin3DHelpers;

            const GPUTextureDescriptorHelpers = __bjs_createGPUTextureDescriptorHelpers();
            structHelpers.GPUTextureDescriptor = GPUTextureDescriptorHelpers;

            const GPUTextureViewDescriptorHelpers = __bjs_createGPUTextureViewDescriptorHelpers();
            structHelpers.GPUTextureViewDescriptor = GPUTextureViewDescriptorHelpers;

            const GPUSamplerDescriptorHelpers = __bjs_createGPUSamplerDescriptorHelpers();
            structHelpers.GPUSamplerDescriptor = GPUSamplerDescriptorHelpers;

            const GPUQuerySetDescriptorHelpers = __bjs_createGPUQuerySetDescriptorHelpers();
            structHelpers.GPUQuerySetDescriptor = GPUQuerySetDescriptorHelpers;

            const GPUPassTimestampWritesHelpers = __bjs_createGPUPassTimestampWritesHelpers();
            structHelpers.GPUPassTimestampWrites = GPUPassTimestampWritesHelpers;

            const GPUBufferBindingLayoutHelpers = __bjs_createGPUBufferBindingLayoutHelpers();
            structHelpers.GPUBufferBindingLayout = GPUBufferBindingLayoutHelpers;

            const GPUSamplerBindingLayoutHelpers = __bjs_createGPUSamplerBindingLayoutHelpers();
            structHelpers.GPUSamplerBindingLayout = GPUSamplerBindingLayoutHelpers;

            const GPUTextureBindingLayoutHelpers = __bjs_createGPUTextureBindingLayoutHelpers();
            structHelpers.GPUTextureBindingLayout = GPUTextureBindingLayoutHelpers;

            const GPUStorageTextureBindingLayoutHelpers = __bjs_createGPUStorageTextureBindingLayoutHelpers();
            structHelpers.GPUStorageTextureBindingLayout = GPUStorageTextureBindingLayoutHelpers;

            const GPUBindGroupLayoutEntryHelpers = __bjs_createGPUBindGroupLayoutEntryHelpers();
            structHelpers.GPUBindGroupLayoutEntry = GPUBindGroupLayoutEntryHelpers;

            const GPUBindGroupLayoutDescriptorHelpers = __bjs_createGPUBindGroupLayoutDescriptorHelpers();
            structHelpers.GPUBindGroupLayoutDescriptor = GPUBindGroupLayoutDescriptorHelpers;

            const GPUMultisampleStateHelpers = __bjs_createGPUMultisampleStateHelpers();
            structHelpers.GPUMultisampleState = GPUMultisampleStateHelpers;

            const GPUVertexStateHelpers = __bjs_createGPUVertexStateHelpers();
            structHelpers.GPUVertexState = GPUVertexStateHelpers;

            const GPUFragmentStateHelpers = __bjs_createGPUFragmentStateHelpers();
            structHelpers.GPUFragmentState = GPUFragmentStateHelpers;

            const GPURenderPipelineDescriptorHelpers = __bjs_createGPURenderPipelineDescriptorHelpers();
            structHelpers.GPURenderPipelineDescriptor = GPURenderPipelineDescriptorHelpers;

            const GPUBufferBindingHelpers = __bjs_createGPUBufferBindingHelpers();
            structHelpers.GPUBufferBinding = GPUBufferBindingHelpers;

            const GPUBindGroupEntryHelpers = __bjs_createGPUBindGroupEntryHelpers();
            structHelpers.GPUBindGroupEntry = GPUBindGroupEntryHelpers;

            const GPUBindGroupDescriptorHelpers = __bjs_createGPUBindGroupDescriptorHelpers();
            structHelpers.GPUBindGroupDescriptor = GPUBindGroupDescriptorHelpers;

            const GPUCanvasConfigurationHelpers = __bjs_createGPUCanvasConfigurationHelpers();
            structHelpers.GPUCanvasConfiguration = GPUCanvasConfigurationHelpers;

            const GPUComputeStateHelpers = __bjs_createGPUComputeStateHelpers();
            structHelpers.GPUComputeState = GPUComputeStateHelpers;

            const GPUComputePipelineDescriptorHelpers = __bjs_createGPUComputePipelineDescriptorHelpers();
            structHelpers.GPUComputePipelineDescriptor = GPUComputePipelineDescriptorHelpers;

            const GPUPipelineLayoutDescriptorHelpers = __bjs_createGPUPipelineLayoutDescriptorHelpers();
            structHelpers.GPUPipelineLayoutDescriptor = GPUPipelineLayoutDescriptorHelpers;

            const exports = {
                GPUCanvasAlphaMode: GPUCanvasAlphaModeValues,
                GPUPrimitiveTopology: GPUPrimitiveTopologyValues,
                GPUVertexFormat: GPUVertexFormatValues,
                GPUVertexStepMode: GPUVertexStepModeValues,
                GPUTextureFormat: GPUTextureFormatValues,
                GPULoadOp: GPULoadOpValues,
                GPUStoreOp: GPUStoreOpValues,
                GPUFrontFace: GPUFrontFaceValues,
                GPUCullMode: GPUCullModeValues,
                GPUIndexFormat: GPUIndexFormatValues,
                GPUBufferBindingType: GPUBufferBindingTypeValues,
                GPUTextureViewDimension: GPUTextureViewDimensionValues,
                GPUTextureSampleType: GPUTextureSampleTypeValues,
                GPUStorageTextureAccess: GPUStorageTextureAccessValues,
                GPUSamplerBindingType: GPUSamplerBindingTypeValues,
                GPUQueryType: GPUQueryTypeValues,
                GPUAddressMode: GPUAddressModeValues,
                GPUFilterMode: GPUFilterModeValues,
                GPUMipmapFilterMode: GPUMipmapFilterModeValues,
                GPUCompareFunction: GPUCompareFunctionValues,
                GPUBlendFactor: GPUBlendFactorValues,
                GPUBlendOperation: GPUBlendOperationValues,
                GPUTextureAspect: GPUTextureAspectValues,
                GPUTextureDimension: GPUTextureDimensionValues,
                GPUStencilOperation: GPUStencilOperationValues,
            };
            _exports = exports;
            return exports;
        },
    }
}
