{
  "name": "adawebsite",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "geo": "node scripts/generate-geo-assets.mjs",
    "rss": "node scripts/generate-rss.mjs",
    "prebuild": "npm run geo && npm run rss",
    "dev": "vite",
    "build": "tsc -b && vite build",
    "pretest": "npm run geo",
    "preview": "vite preview",
    "test": "node src/routing.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^5.4.10"
  },
  "dependencies": {
    "highlight.js": "^11.11.1"
  }
}
